╔══════════════════════════════════════════════════════════════════════════════╗
║ SPEC GẮN data-region — MODW (modw-builder-v1.html)  ·  Loại A bước B2          ║
║ Soạn: CW (Cowork) · 2026-07-15 · TRÌNH DESKTOP SOI (B3) trước khi giao Claude Code ║
║ Nguồn: /opt/incomex/docs/mcp-writes/ui-preview/modw-builder-v1.html (đọc LIVE)  ║
╚══════════════════════════════════════════════════════════════════════════════╝

── 0. PHẠM VI (đã chốt với Desktop) ─────────────────────────────────────────────
Bản đồ có 31 mã modw.* = 3 NA gốc-cây + 3 chưa-dựng + 25 mã CẦN GẮN (spec này).
  • NA gốc-cây (KHÔNG gắn): modw (128) · modw.build (132) · modw.picker (152)
  • Chưa-dựng (KHÔNG gắn, không có element): modw.build.approver (156) ·
    modw.build.delegate (157) · modw.build.variant (169) — GIỮ anchor_scope=PLANNED.
  • Dock (shell.dock.*) do eco-nav.js inject → prefix shell.*, KHÔNG phải modw.* → tự ngoài rổ.
MATCHES dưới đây ĐẾM OPEN-TAG trên raw live (đã grep xác minh: 6 .psec · 2 .addsm ·
  2 .tb-link · el chip=1 · el storechk=1 · el pitem=2 · mọi id duy nhất=1).

── QUY ƯỚC ──────────────────────────────────────────────────────────────────────
STRING = FIND→REPL literal (chèn ` data-region="…"` vào thẻ). OCC:first (MATCHES=1).
DOM    = setAttribute NGAY SAU dòng tạo thẻ (thẻ sinh bằng el()/createElement).
Claude Code: thay ĐÚNG LITERAL, KHÔNG đoán. FIND không khớp/khớp≠MATCHES → DỪNG, báo CW.

════════════════════════════════════════════════════════════════════════════════
 NHÓM 1 — STRING (21 mã) · MATCHES=1 · OCC:first · Luật L3
════════════════════════════════════════════════════════════════════════════════

# id 129 · modw.shell  (topbar)
FIND: <div class="topbar">
REPL: <div class="topbar" data-region="modw.shell">

# id 130 · modw.shell.link.canvas
FIND: <a class="tb-link" href="./mow-unified-canvas-v2.html">
REPL: <a class="tb-link" href="./mow-unified-canvas-v2.html" data-region="modw.shell.link.canvas">

# id 131 · modw.shell.link.list   (giữ nguyên style= phía sau)
FIND: <a class="tb-link" href="./mow-master-v1.html"
REPL: <a class="tb-link" href="./mow-master-v1.html" data-region="modw.shell.link.list"

# id 133 · modw.build.node.name   (giữ nguyên value= phía sau)
FIND: <input id="nNode"
REPL: <input id="nNode" data-region="modw.build.node.name"

# id 134 · modw.build.node.tier
FIND: <select id="nTier"
REPL: <select id="nTier" data-region="modw.build.node.tier"

# id 135 · modw.build.parent
FIND: <div class="pickfld" id="parPick"
REPL: <div class="pickfld" id="parPick" data-region="modw.build.parent"

# id 136 · modw.build.exec   (KHU = CẢ MỤC 3 → neo .psec, anchor dài số+tiêu đề, trùm header+chips+nút thêm)
FIND: <div class="psec"><div class="phd"><span class="no">3</span> Người thực hiện
REPL: <div class="psec" data-region="modw.build.exec"><div class="phd"><span class="no">3</span> Người thực hiện

# id 138 · modw.build.exec.add
FIND: <button class="addsm" onclick="UI.openPeople('exec')">
REPL: <button class="addsm" onclick="UI.openPeople('exec')" data-region="modw.build.exec.add">

# id 139 · modw.build.recipient  (KHU = CẢ MỤC 4)
FIND: <div class="psec"><div class="phd"><span class="no">4</span> Người nhận báo cáo
REPL: <div class="psec" data-region="modw.build.recipient"><div class="phd"><span class="no">4</span> Người nhận báo cáo

# id 141 · modw.build.recipient.add
FIND: <button class="addsm" onclick="UI.openPeople('recip')">
REPL: <button class="addsm" onclick="UI.openPeople('recip')" data-region="modw.build.recipient.add">

# id 142 · modw.build.store   (KHU = CẢ MỤC 5)
FIND: <div class="psec"><div class="phd"><span class="no">5</span> Lưu ở bảng nào (kho)
REPL: <div class="psec" data-region="modw.build.store"><div class="phd"><span class="no">5</span> Lưu ở bảng nào (kho)

# id 144 · modw.build.time
FIND: <input id="nWhen"
REPL: <input id="nWhen" data-region="modw.build.time"

# id 145 · modw.preview   (node card bản đúc)
FIND: <div class="nodecard">
REPL: <div class="nodecard" data-region="modw.preview">

# id 146 · modw.preview.cast
FIND: <button class="castbtn" onclick="App.cast()">
REPL: <button class="castbtn" onclick="App.cast()" data-region="modw.preview.cast">

# id 147 · modw.preview.tier
FIND: <span class="tchip" id="cTier">
REPL: <span class="tchip" id="cTier" data-region="modw.preview.tier">

# id 148 · modw.preview.name
FIND: <span class="nc-name" id="cName">
REPL: <span class="nc-name" id="cName" data-region="modw.preview.name">

# id 149 · modw.preview.bread
FIND: <div class="nc-bread" id="cBread">
REPL: <div class="nc-bread" id="cBread" data-region="modw.preview.bread">

# id 150 · modw.preview.body
FIND: <div class="nc-bd" id="cBody">
REPL: <div class="nc-bd" id="cBody" data-region="modw.preview.body">

# id 151 · modw.preview.json   (tên UI "Khuôn đúc"; KHÔNG đưa chữ JSON lên UI)
FIND: <pre class="json-out" id="moldJson">
REPL: <pre class="json-out" id="moldJson" data-region="modw.preview.json">

# id 153 · modw.picker.title
FIND: <h3 id="pickTitle">
REPL: <h3 id="pickTitle" data-region="modw.picker.title">

# id 154 · modw.picker.close   (neo onclick — KHÔNG neo class "x" vì "x" chung)
FIND: <button class="x" onclick="UI.closePick()">
REPL: <button class="x" onclick="UI.closePick()" data-region="modw.picker.close">

════════════════════════════════════════════════════════════════════════════════
 NHÓM 2 — DOM setAttribute (4 mã / 3 dòng render) · ⚠️ ĐỔI LOGIC → MỞ UI KIỂM
 (byte-check SHA sẽ KHÔNG trùng — bình thường; nghiệm thu phải mở UI thật)
════════════════════════════════════════════════════════════════════════════════

# id 137 + 140 · modw.build.exec.chip / modw.build.recipient.chip
#   Luật L2 — 1 vòng render sinh CẢ 2 loại chip (khác Ý NGHĨA) → truyền mã theo `kind`.
#   Trong UI.chips, dòng: var c=el('div','chip');      (MATCHES=1)
#   CHÈN NGAY SAU dòng đó:
      c.setAttribute('data-region', kind==='exec'?'modw.build.exec.chip':'modw.build.recipient.chip');

# id 143 · modw.build.store.item
#   Luật L4 — 1 dòng render 6 kho (STORES 6 phần tử).
#   Trong UI.storeList, dòng: var row=el('label','storechk');   (MATCHES=1)
#   CHÈN NGAY SAU:
      row.setAttribute('data-region','modw.build.store.item');

# id 155 · modw.picker.item
#   Luật L4 — 2 dòng render (nhánh chọn-bố + nhánh chọn-người), CÙNG concept → OCC:ALL.
#   Trong UI.pickRender có 2 dòng: var it=el('div','pitem');   (MATCHES=2 — gắn CẢ HAI)
#   CHÈN NGAY SAU mỗi dòng:
      it.setAttribute('data-region','modw.picker.item');

════════════════════════════════════════════════════════════════════════════════
 CÂU LỆNH TRỌNG TÀI CHUẨN (LUẬT C — huong_dan d222) · tái dùng cho CẢ 7 UI
 → Ghi vào quy_trinh sau khi Desktop soi (task #13). Thay <PREFIX> + <FILES>.
════════════════════════════════════════════════════════════════════════════════
# Source (mã trong code sống):
  grep -rho 'data-region="<PREFIX>\.[^"]*"' <FILES> | sed 's/.*="//; s/"$//' | sort -u > /tmp/src.txt
# Excel (cột C lọc theo LUẬT C — tự loại dock shell.* + chưa-dựng NOT_BUILT):
#   {ma_khu LIKE '<PREFIX>.%'  AND anchor_scope='DOM'  AND trang_thai='BUILT'}  → /tmp/xls.txt
# Đối chiếu 2 chiều (rỗng = 0 thiếu/0 thừa):
  comm -3 /tmp/src.txt /tmp/xls.txt
# MODW: <PREFIX>=modw · <FILES>=modw-builder-v1.html (+eco-nav.js chỉ chứa shell.* → không lọt modw.)
#   Kỳ vọng sau gắn: 25 mã hai bên khớp. (Dock 6 = shell.* → ngoài rổ. 3 chưa-dựng = NOT_BUILT → ngoài rổ.)

════════════════════════════════════════════════════════════════════════════════
 SAU KHI GẮN (B5 CW nghiệm thu)
════════════════════════════════════════════════════════════════════════════════
1. Trọng tài (câu lệnh trên) = 25, 0 thiếu/0 thừa.
2. Byte-check: gỡ attr STRING phải hiện lại FIND gốc (SHA trùng). Nhóm DOM: SHA KHÔNG trùng
   là BÌNH THƯỜNG → MỞ UI THẬT, kiểm chip exec vs recip ra đúng 2 mã, store 6, picker item.
3. Backup Excel → đổi anchor_scope 25 mã: PLANNED→DOM (giữ 3 chưa-dựng = PLANNED).
4. Codex chụp màn 11,12 → cập nhật man_hinh.
5. Siết concept MODW (kèm ảnh) — KHÔNG chặn bước gắn.

── TỔNG: 25 mã (21 STRING + 4 DOM). NT1–6 áp nguyên. Chưa giao Claude Code khi Desktop chưa soi. ──

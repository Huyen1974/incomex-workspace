# Nguồn, phạm vi và lỗi chuẩn bị được giữ

- SSOT đọc trên ổ đĩa: v1.6.10, SHA256 `7cd44d675bed2fff179b74d9ded190af50627406db7abda257a67ec838111621`. Lệnh PM mới là nguồn nghiệm thu retrieval; file PM_review_Proof_A riêng được nhắc nhưng không tìm được attachment. Không nói đã đọc file đó.
- AGENTS local và `.claude/skills/incomex-rules.md` đã đọc; query MCP Agent Data trực tiếp để tìm operating rules/constitution. Tool get_document chỉ trả summary truncated (VPS rules rev2; Constitution v4.6.3 rev44), không claim đã đọc đầy đủ KB. Lệnh hiện tại cho phép fixture/schema/lab và yêu cầu không ghi production/KB, nên không thực hiện COMMENT ON/OR upload hoặc deploy theo workflow cũ.
- E05/E06 và bốn ô retrieval PM đã nhận được dùng lại; chỉ chạy sáu Search Brief requests hiện tại bằng A1 nguyên hash, không benchmark tuning hoặc vector.
- `source-current.json` giữ lỗi read-only đầu do giả tên cột name trong directus_extensions; transaction bị rollback. Query sửa bằng introspection và source thực nằm ở `source-database.json`. Không query business records.
- Runtime build current hash được đọc; local source checkout cũ chỉ dùng lần theo component. Git HEAD ở thư mục VPS thưa source có thể thuộc parent repository, không dùng làm provenance build. Nuxt nitro manifest/current compiled source mới là bằng chứng module đang deploy.
- Bootstrap lab r1 lỗi email đuôi invalid: toàn bộ owned lab trống được tạo lại, không có business tests. R2 chạy được app; Docker29 internal network không xuất port ra host nên dùng IP container qua host/SSH. Không mở public network để chữa lỗi. 503 health do tmpfs root-owned được khắc phục bằng chown đúng tmpfs của lab sang UID1000; không có uploads/personal data.
- Một lần gọi validator xảy ra khi transfer input chưa hoàn tất, JSON input rỗng gây parse error. Chưa có validator result; sau transfer đầy đủ đã chạy đúng Joi17.13.3, giữ nguyên profile/expected. Đây là lỗi điều phối của Agent, không phải lỗi sản phẩm và không được giấu trong tỷ lệ PASS.
- Directus /versions v11.5 không nhận id do caller đưa: giữ lần đầu HTTP400. R2 bỏ duy nhất thuộc tính id của API envelope và để Directus cấp ID, không thay form/schema/key hoặc expected.
- Hai request chống sai đã thực sự vượt cổng trong lab: stale If-Match và nested contract_ref giả. Hai lần khôi phục fixture sau chụp before/after để kiểm các nhánh độc lập được ghi trong HTTP/audit; không sửa implementation để đổi kết quả FAIL thành PASS.
- `integration-results.json.tests[].mutation` quan sát forms/MOW/MOT/records/fields/versions; snapshot này không gồm checkpoint. Vì vậy checkpoint write có `mutation:false` trong raw harness dù API trả200. PG checkpoint readback và fresh-agent tool là bằng chứng riêng; không diễn giải cột đó thành toàn DB không mutation.
- X-Request-ID của harness chưa được chứng minh persist trong Directus Activity. Truy vết bằng request log + ID/item + authenticated user + timestamp + activity/revision; không claim full correlation/delegation audit. Delegation chưa được thử.
- Không tạo adapter/renderer/runtime engine. Mọi file mới là test harness/profile/config/evidence; không gọi chúng là zero-code preparation. Business production code mới = 0.

- Version r2 không đủ quyền field `hash` do Directus tự sinh. Lab policy r3 chỉ thêm hash cho service create/update directus_versions, không admin/actor/approve; ca giả user_created, ngoài collection và published được thử lại, kết quả chặn nhưng ngoài collection là 422 feature-disabled, chưa chứng minh riêng policy-scope.
- R3 `PATCH /items/forms/:id?version=notes-v2` sửa main: đây là lỗi chọn endpoint của harness, không kết luận content-versioning native bị hỏng. Đã đọc controller/service exact11.5: `POST /versions/:id/save` mới là cổng lưu delta. R4 khôi phục fixture main sau chụp bằng chứng, rồi save đúng endpoint: main giữ nguyên, version có ghi chú, MOT vẫn main. Các request r1–r4 và lỗi đều giữ.
- Native VersionsService.save ghi draft delta; source chưa thấy cùng validator item/profile được áp lên delta tại save. Không coi native save là contract admission/publish authority đã chứng minh. Native promote có mainHash check trong source nhưng không gọi promote trong lượt này; If-Match trên PATCH item thất bại không chứng minh mọi cơ chế concurrency Directus đều thiếu.

- Transfer validator-output có hai dạng JSON compact/pretty cùng nội dung; giữ cả hai bytes, hash remote và mapping tại transfer-verification.json. Code inventory đếm file harness lưu trong archive, không tổng mọi lệnh đọc nguồn/soạn báo cáo của phiên. Script sửa SSOT được liệt kê riêng.

- PG lab A/B dùng image current16.13 Debian trixie, locale actual en_US.utf8. Lượt retrieval trước có môi trường riêng được lưu trong archive cũ; lượt này giữ SQL/config A1 theo hash nhưng không tuyên bố tái hiện toàn bộ môi trường benchmark hoặc so lại latency hai lượt.

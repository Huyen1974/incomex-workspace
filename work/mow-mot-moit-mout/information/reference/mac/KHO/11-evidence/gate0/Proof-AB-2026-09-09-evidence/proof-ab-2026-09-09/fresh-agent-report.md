# Fresh-agent resume từ checkpoint

Nguồn duy nhất: JSON do `resume-readonly.py proof-ab-duration-checkpoint` trả về, đã lưu nguyên tại `fresh-agent-readback.json`. Lệnh đọc hoàn tất với mã 0; danh tính phiên mới là service-author `2ace37b1-2468-485c-9788-51517c5e8550`, không phải approver. Không đọc lịch sử task hay báo cáo trước đó.

- Đã đọc được checkpoint `proof-ab-duration-checkpoint`, MOW `1e1bae69-69e0-5502-927f-0913891ed78b`, MOT `51ac625c-071f-5998-8e07-09bd61613184`, form `16a85219-1758-59f5-acef-e7d752edb36c` (key `proof-duration`) và profile `form-contract-profile-v1`, version 1. Checkpoint/MOW/MOT/form đều ở trạng thái draft, scope pilot.
- Chuỗi binding hiện có: checkpoint → MOW → MOT → form; MOT vẫn chọn `form_version: main`. Checkpoint yêu cầu `notes-v2`. Bản `form_selected_version` trả về giữ cùng ID/key và có duration + note, cùng system field ngày tạo; `form_main` chỉ có duration và system field ngày tạo. Payload bản được chọn không tự mang khóa version, nên tên `notes-v2` được xác định theo checkpoint, không phải metadata version độc lập.
- Ba Field reference trong bản được chọn là `field.so_phut`, `field.ghi_chu`, `field.ngay_lap`; các contract reference lần lượt là `duration-minute-v1`, `note-v1`, `created-at-v1` (đều version 1). JSON chứng minh các reference được lưu trong form; không có bản ghi Field/contract riêng để xác nhận độc lập tài nguyên đó. Checkpoint ghi nhận ba Fields cùng family và native save/readback PASS trong phạm vi hẹp; đây là kết quả lưu tại checkpoint, không phải phép thử tôi vừa chạy.
- Phần còn thiếu theo checkpoint: rà soát cưỡng chế contract phía server và stale revision (proof ghi server JSON contract/stale item PATCH failed); Nuxt inbox chưa thực hiện ghi/đọc lại runtime theo contract; UI/runtime/release chưa hoàn tất; chưa thiết lập release MOW có quản trị. Content version hiện là draft delta trên main có thể thay đổi, chưa chứng minh pin phiên bản MOW đã publish bất biến. Chuyển đổi hour → minute chưa được phê duyệt.
- Next action hợp lệ là `review-gaps-before-publish` với form hiện có và version yêu cầu `notes-v2`: đối chiếu các thiếu hụt trên trước khi cân nhắc đổi binding hay publish. Không tạo lại MOW, MOT, form hoặc Fields; không publish ở trạng thái hiện tại. Tôi không thực hiện next action, không đổi binding, không gọi runtime và không ghi business data.

Kết luận giới hạn: có thể khôi phục ngữ cảnh tác nghiệp và phân biệt bản nội dung được yêu cầu với binding đang hoạt động từ checkpoint/readback. Kết quả không xác nhận full T0 hay runtime PASS; proof tổng thể vẫn PARTIAL theo checkpoint.

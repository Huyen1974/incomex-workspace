# Platform Freeze · kết luận có phạm vi

**TARGET STACK PARTIAL — exact follow-up.** Đề nghị giữ PG16.15, Directus12.3.1, Nuxt4.5.2 làm technical candidates; Gate2 thiết kế theo contract tương thích CURRENT, không nhận TARGET đã được cài production. Không khuyến nghị một đợt xây dài hạn trên CURRENT thiếu hạn. PM xem lại platform trước Gate3 contract lock và trước triển khai, muộn nhất 18/09/2026 nếu chưa có dữ kiện mới. Đây là hạn review đề nghị, không lịch automation hay Owner acceptance.

## Đã refresh và chạy gì

Nguồn chính thức refresh 11/09/2026, 11 URL trả200; exact candidates không đổi so gói trước. PG giữ major16 và chọn16.15; Directus12.3.1; Nuxt4.5.2; worker Node22.23.2; Nuxt Node24.21.0. Image Directus12 thực dùng Node22.23.2/API39.1.0 (`target-directus-runtime-version.json`); ephemeral probe không network đã tự thu hồi. Image digest ở `remote-target/target-image-pins.json`, Node local verified official SHASUMS ở `local-node.json`. [PG16.15](https://www.postgresql.org/docs/release/16.15/), [Directus12.3.1](https://github.com/directus/directus/releases/tag/v12.3.1), [Nuxt4.5.2](https://github.com/nuxt/nuxt/releases/tag/v4.5.2), [Node index](https://nodejs.org/dist/index.json).

Hai lab VPS2 tách network internal, DB, volume/tmpfs và credential; synthetic data; không public port hay egress integration. Target: PG16.15 + Directus12.3.1. Bridge độc lập: PG16.15 + accepted Directus11.5.1 (Node của image22.14.0), Nuxt4.5.2/Node24.21.0 và worker Node22.23.2. Tên bridge luôn ghi rõ trong UI/results. Không thay demo e-learning, không migrate/deploy/restart production.

| Lát cắt | Kết quả thực tế | Giới hạn / bằng chứng |
|---|---|---|
| Directus12 native | Ping200; tạo synthetic item, version và compare200; PG/storage health OK | `remote-target/target-native-probe.json`; admin chỉ bootstrap/probe native, không là business proof. Overall health503 vì SMTP bị khóa ở loopback port9. |
| Directus12 scoped permission | **403 RESOURCE_RESTRICTED: custom_permission_rules_enabled** khi POST permissions | `remote-target/requests.jsonl`, `license-enforcement-finding.md`. Full guard slice dừng ở prerequisite quyền licensed; không kết luận hook/ItemsService fail và không bypass/giảm contract. |
| PG16.15 + Directus11.5.1 guard | **66/66** + **1/1 rollback sau native write** | `remote-bridge/gate-results.json`, `rollback-result.json`: human/service/outsider,422/403/409, concurrency, merged validation, version protection, replay/audit, rollback business+audit khi receipt thất bại. Đây là CURRENT CMS bridge. |
| Nuxt4.5.2 + Node24.21.0 | Build cuối thành công; SSR cookie non-admin; Form/DirectusTable đọc được; declaration revision7→8 thêm note không đổi27 source files | `ui-unchanged-v1-v2.json` có count chính xác; `ui-ssr-session-results.json`/HTML, browser/UI log. Native httpOnly session/Lax, Secure=false chỉ loopback; không SSO/CSRF/production session proof. |
| UI ghi/đọc/lỗi | 6 POST:422,200,200,409,200,200(replayed) | `ui-action-results.json`: query số dạng string bị422; nhập number control ghi/read-back;409 giữ input, re-read nhập lại; replay không tăng revision. Native table có3 contracts; fields/proposals403 được giữ, không cấp thêm quyền chỉ để làm đẹp. |
| Worker Node22.23.2 / pg-boss12.30.0 / pg8.23.0 |3 occurrence/3instance/3job hoàn thành, mỗi job1attempt/1record; duplicate trả cùng IDs; changed payload conflict; restart giữ queued V1, binding đổi chỉ event mới V2 | `runtime-rehearsal-results.json` và snapshots. Kernel/guard logic cùng byte accepted; producer/worker không superuser/DDL/bypassRLS, không sửa release/direct business table. |
| Pin khi Form main đổi | **Counterexample xác nhận**: event4 giữ releasev2 nhưng main revision8→9,2attempts failed FORM_REVISION_CONFLICT; không có effect | `pin-definition-negative.json`. Snapshot release chưa đủ full pinned semantics; không sửa gate để né. Cần release-aware material read contract tại Gate3. |
| PG minor / backup-restore | btree_gist1.7 range smoke + exclusion overlap reject; dump117017bytes restore vào DB mới, counts/ranges/indexes khớp | `remote-target/pg-minor-restore-results.json`. Synthetic restore, không là in-place production migration proof. |

## Những vấn đề phát hiện và cách xử lý có giới hạn

Nuxt UI2.22.3 đang sinh selector CSS bị Lightning CSS của Vite8.3.0 từ chối. Dùng tùy chọn native `cssMinify: esbuild`, không sửa component. Harness ban đầu thiếu presentation/signature packages nên thêm đúng dependency đã có trong nguồn: auto-animate0.8.4, vue-dompurify-html5.3.0, v-perfect-signature1.4.0; @nuxt/image1.11.0. Vue3.5.25 của CURRENT thấp hơn dependency Nuxt4 (^3.5.40), gây thiếu runtime file khi Nitro trace; đồng bộ Vue3.5.42 theo package thực rồi build/SSR đạt. Các lỗi đầu giữ nguyên log. [Vite build options](https://vite.dev/config/build-options), [Nuxt tagged source](https://github.com/nuxt/nuxt/tree/v4.5.2).

**Module compatibility còn PARTIAL:** npm audit báo4 findings (1moderate,3high), không phải4 lỗ hổng độc lập; graph gồm @nuxt/ui, @nuxt/image, ipx, sharp. Audit đề nghị UI4.11.1 và Image2.1.0 là major. Không chạy force upgrade: phải có một rehearsal riêng giữ FormCustom/UI behavior trước khi chọn migration. Sharp binary warning còn; upload/signature/image processing không nằm trong duration/note slice. CSS fallback là nợ tương thích, không kết luận entire product build PASS. Evidence: `ui-npm-audit.json`, package-lock và build logs.

**Current query-prefill chưa typed:** '37' từ URL bị422 đúng server contract; action scope/revision/context không được tin từ query. Gate3 khai allowlist+type conversion chung theo schema hoặc reused prefill composable; giữ server validation. Current localStorage adapter key theo form chưa đủ identity/scope, session reauth/expiry/CSRF và dirty-state reconcile cần proof. Không cần renderer riêng cho từng form.

**Release pin:** current guard kiểm Form main; case drift đã chứng minh không được nhận full immutable release. Thiết kế đích: guard runtime đọc đúng material closure theo approved release (native version read nếu đủ, hoặc release snapshot tối thiểu được quản trị), vẫn recheck quyền hiện hành. Before Pilot cần case sửa/retire main trong khi instance cũ còn chạy, capability hash đổi, profile/contract đổi, quyền bị thu hồi, effect unknown. Gate2 chỉ khóa đường đi/ranh giới; Gate3 quyết exact contract sau inventory, không sinh production tables/SQL ở đây.

**PG actual material:** production read-only metadata cho PG16.13, btree_gist1.7/pgcrypto1.3/plpgsql1.0/postgres_fdw1.1;0 slots;3GiST indexes và không ltree. Query đầu regex overmatch “registry”, đã giữ raw và sửa bằng pg_am (`production-pg-material-corrected.json`). Release16.15 nêu reindex btree_gist float4/8 nếu có NaN và ltree btree với đường rất dài; không suy mọi GiST cần reindex hoặc bỏ qua chỉ từ tên cột. Opclass/type/NaN/material review cụ thể phải có trong production minor upgrade plan; không chạy reindex production trong nhiệm vụ này. [PG release notes](https://www.postgresql.org/docs/release/16.15/).

## TARGET built-in fit trước khi giữ custom

| Chức năng cần | Native ưu tiên | Quyết định có evidence |
|---|---|---|
| Type/FK/unique/temporal integrity | PG native constraints/index/transaction | Reuse; không duplicate validator vật lý ở Nuxt. Business semantic profile multi-ref không tự có từ type/FK. |
| Item lifecycle/version/diff | Directus versions/compare/promote(mainHash), activity/revisions | Target native probe200; dùng cho phần fit. Một item version không mặc nhiên là release closure nhiều object. [Native versioning](https://docs.directus.io/guides/headless-cms/content-versioning), [SDK promotion](https://docs.directus.io/packages/%40directus/sdk/rest/functions/promotecontentversion). |
| Auth/session/accountability | Directus roles/policies/ItemsService/native session | Reuse. Native session được test bridge; license feature prerequisite12 còn mở. Không tự viết auth engine. [Session modes](https://docs.directus.io/reference/authentication). |
| Blocking validation / flow | Directus filter Flow phù hợp checks đơn giản; native conditions/operations | Chưa chứng minh thay được atomic merged semantic validation + CAS + replay + scoped material + receipts trong accepted guard. RunScript là code, không là config/no-code; module/network restrictions làm nó không tự thay worker. [Flow operations](https://docs.directus.io/app/flows/operations). |
| Dynamic form/table | Existing Nuxt Form/UForm/FormCustom/DirectusTable + SDK | Reuse source; declaration đổi renderer không đổi. Need typed-prefill/context/session and UI2 migration proof, không page theo từng object. |
| Durable queue/retry/ack | pg-boss native | Reuse; code kernel capability vẫn phải đếm. Test Node22.23.2 passed bounded; không viết claim/ack mới. Existing queue functions chỉ discovery trước, không bật production substrate. |

## License precondition và việc cần chốt tiếp

Owner đã xác nhận revenue dưới ngưỡng; **không hỏi lại revenue**. Còn headcount theo phạm vi pháp nhân/chương trình, phạm vi sử dụng, số Studio users và key/activation hợp lệ cho tính năng scoped custom permissions. Core miễn phí tồn tại theo thông báo v12 nhưng actual licensed feature gate đã xảy ra; không được dùng khả năng bootstrap native để kết luận mọi quyền miễn phí. Không đăng ký OIG, không chấp nhận terms, không gửi thông tin doanh nghiệp, không nhập/mua key trong đợt này. [Directus v12 license change](https://directus.com/resources/directus-v12-license-change), [OIG](https://directus.com/oig), [License](https://directus.com/license).

Follow-up trước adoption: (A) xử lý hợp lệ custom permission feature prerequisite và chạy nguyên slice66+rollback+UI/runtime trên12; (B) giải module UI/Image audit + SSR/cookie/dirty-refetch bằng reuse trước; (C) release-aware contract/pins proof theo counterexample; (D) production PG minor plan/backup/restore/material index check. PM quyết technical direction/adoption; Owner quyết legal/authority facts cần thiết. Các việc này không chặn Gate2 design review, nhưng chặn tuyên bố production-ready/full-target-pass.

## Thu hồi lab

Đã kiểm39 target +98 bridge public evidence files bằng hash trước dọn. Hai network/lab containers/volumes và private data đã dọn; không prune shared images.5 demo containers giữ nguyên ID/image/start/restart/state,2 safe-GET responses/hash/location khớp. Local Nuxt/tunnel đã dừng, synthetic credential file đã xóa. Các test DB dump chứa credential tổng hợp không đưa vào ZIP; proof restore/hash đã giữ. `cleanup-results.json`, `local-cleanup.json`, `lab-export-verification.json`. Không suy safe GET bằng full e-learning regression.

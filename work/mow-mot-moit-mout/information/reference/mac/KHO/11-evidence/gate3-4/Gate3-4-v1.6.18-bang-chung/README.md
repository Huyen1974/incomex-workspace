# W005 Revision2 · v1.6.18 — đọc báo cáo trước

Kết luận SUBMITTED / BLOCKED BY DATA-AUTHORITY; Gate3/4 PARTIAL, chưa Gate5. Mở `gate3-4/BAO-CAO-GUI-PM.md`; supervisor mở `gate3-4/SUPERVISOR-REVIEW-INPUT.md`. File HTML ở gốc là exact SSOT snapshot; `backup/` giữ exactv17. `manifest-sha256.json` kiểm mọi payload file trong ZIP.

ZIP giữ toàn bộ public current proof: original prompts/full tools/PG before-after, compiler/config/guard source và lệnh replay đã kiểm. `gate3-4/README.md` chỉ cách tiếp tục và giới hạn; không phải SSOT thứ hai. `workshop/` vẫn TEST design, không connected runtime.

Trong dự án, current links ở SSOT trỏ thư mục đã giải nén. Tệp ZIP là gói chuyển cho Chat/supervisor, không chứa lại9 ZIP lịch sử và không phải website tự chứa mọi link. HTML snapshot giữ nguyên link project-relative để bảo toàn byte; một số cross-project/history links cần mở từ thư mục dự án gốc. Full proof source chỉ cần giải nén ZIP nếu chạy lại; không đọc toàn bộ để tiếp cận.

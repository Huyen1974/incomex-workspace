# Bản đồ từng bước · Pha B · v1.6.20

## Chỉ đạo hiện hành · menu và ảnh lớn · 12/09/2026

**CHỜ OWNER DUYỆT MẪU:** [Mở TAO-MOT-QUY-TRINH.html tại thư mục gốc](../../../../TAO-MOT-QUY-TRINH.html). Đây là bản trình bày02 hiện hành, sửa tại đúng tên này; Owner chỉ cần mở HTML đó. Bản đồ Pha B giữ đối chiếu kỹ thuật, chưa được nhận làm cách đọc chuẩn cho người mới.

Owner: người làm được bằng giao diện là đường gốc để xét khả thi, máy tự động hóa cùng hành động/dữ liệu/quyền. [Lệnh menu/ảnh lớn hiện hành](../mau-de-duyet/lenh-menu-anh.md) thay [lệnh mẫu trước](../mau-de-duyet/lenh-pm.md) trong lượt này. Chỉ một mẫu; không mở luồng khác hoặc xây dải UI đang thiếu.

**Đã sửa:** menu bốn chặng, thẻ rộng, ảnh cắt giữ pixel nguồn và khung đỏ; bấm mở ảnh gốc.18việc chính +1nhánh6d =19thẻ;0CÓ/11CÓ–CẦN SỬA/8THIẾU/0KHÔNG CẦN MÀN. Bước6 tách6a biểu mẫu/tham khảo/hướng dẫn,6b người/máy đủ nghĩaD04,6c kết quả/lưu/bàn giao,6d chỉ khi thiếu, giữ người nhận yêu cầu và đường về đúng chỗ.14/16 tái dùng cùng C40 theo dõi ở hai trạng thái; nhãn cần sửa dựa surface đã xem, không suy từ404. Số đếm chỉ của mẫu, không toàn hệ hoặc số màn duy nhất.

**Kiểm:** đã mở chỉ đọc các vùng Ai làm & ai nhận, Chạy & kết thúc, C40 Vận hành, Nháp2; không lưu/duyệt/bật/chạy. Ảnh tìm được chụp lại sau khi phát hiện bản trước thu nhỏ. Đã kiểm crop, mapping, link/menu/nhãn/số đếm và cú pháp JS. **Chưa visual PASS**: chính sách Browser Use file:// đã chặn đọc tài liệu local; không dựng server/đổi trình duyệt để vượt chặn.1280×720/100%, desktop hẹp/điện thoại, bàn phím/dialog mới kiểm mã, chưa được render/interaction nghiệm thu. Giám sát kiểm trên bản bàn giao; Owner chỉ xét cách hiểu và logic.

**Một bản hiện hành:** root là current; đường cũ `../mau-de-duyet/TAO-MOT-QUY-TRINH.html` chỉ dẫn về root. Bản cũ giữ nguyên trong [một ZIP backup](../../../10-backup-tai-lieu/mau-truoc-menu-2026-09-12.zip), kèm SSOT/bản đồ trước đăng ký link và hash. SSOT/bản đồ chỉ thêm mỗi nơi một đoạn đăng ký tài liệu; phần còn lại giữ nguyên byte, dataset/Gate/kiến trúc không đổi. Version kiến trúc v1.6.20 giữ nguyên, bản trình bày02 ghi trong mẫu.

**Hồ sơ nhỏ trong `../mau-de-duyet/`:**
- `lenh-menu-anh.md`, `kiem-menu-anh-ui.json`, `kiem-menu-anh.json`: lệnh mới, quan sát UI, mapping/crop/hash/giới hạn và biên nhận. Đọc đúng file khi cần kiểm nhận định.
- `anh/11`…`16`: ảnh nguồn mới ngày12/09; ảnh01–10 là nguồn lần trước, giữ phục vụ lịch sử. Không chỉnh ảnh evidence gốc; crop/khung đỏ được dựng trong HTML.
- `dung-mau-menu.py`: một chương trình dựng tài liệu từ bản trước trong ZIP và nguồn ảnh; chỉ sinh staging trong `/tmp`, không ghi root/SSOT. Khi sửa format dùng chương trình này hoặc đồng bộ thẻ/menu/card-data; kiểm lại trước cập nhật root. Không code sản phẩm.
- `TAO-MOT-QUY-TRINH.html`: trang chỉ dẫn, không bản current thứ hai.
- `lenh-pm.md`, `kiem-ui.json`, `kiem-tai-lieu.json`, ảnh01–10: **hồ sơ mẫu cũ**; các số16dòng/8ảnh/hash chỉ áp bản đã backup, không tình trạng root mới.

**Ba nội dung vẫn chờ nhập SSOT ở lần cập nhật kiến trúc sau:** (1) nghiệm thu cần kiểm kỹ thuật và xác nhận người hiểu, không thay bằng hash/số dòng; (2) hướng dẫn theo format Owner chọn ở trước, dataset/trace phía sau; (3) D04 đã Owner chấp thuận, Gate1 completeness chưa đóng, duyệt mẫu không tự đóng Gate/chứng minh vận hành. Lần này chỉ đăng ký link theo ngoại lệ PM, chưa tự nhận Owner đã duyệt format.

**Việc tiếp:** chờ phản hồi cách hiểu, chỉnh mẫu tại tên root cố định; chưa nhân rộng. Không yêu cầu tải ZIP46MB hoặc đọc kho kỹ thuật để xét mẫu.

---

## Hồ sơ Pha B đã nộp trước mẫu (giữ nguồn lịch sử)

Điểm đọc duy nhất cho người: [BAN-DO-BUOC-UI-AGENT.html](../../../../BAN-DO-BUOC-UI-AGENT.html). Bản đồ giữ ma trận chi tiết, SSOT giữ kiến trúc/quyết định/trạng thái; không coi snapshot trong gói là nguồn làm việc thứ hai.

**Đã nộp rà Pha B; còn blocker độ phủ lịch sử.** Owner ngày12/09 chỉ đạo tiếp tục từ Checkpoint0.87bước/987lượt gắn giữ nguyên.63bước cần UI:0đủ toàn yêu cầu/51thiếu/10chưa có/2chưa xác minh;24bước máy có nơi quan sát dùng chung.30vùng:22sửa-ghép/7thiết kếbù/1xácminh nguồn. Tool:10asset/source,0đường tự động đầy đủ;84bước có primitive/source liên quan,3chưa dẫn primitive. Không lấy asset count làm automation rate.

## Đọc ít để tiếp tục

1. Mở bản đồ → bước bị ảnh hưởng → vùng UI/tool/contract được dẫn. Chỉ đọc nguồn đó; không đọc lại toàn kho.
2. [SUPERVISOR-REVIEW-INPUT.md](SUPERVISOR-REVIEW-INPUT.md) nêu đúng câu PM/Fible cần kiểm; chưa có independent review.
3. Blocker: nguồn lịch sử23bước thiếu nguyên văn9hàng; currentHTTP vẫn trùng14. PM cần tìm snapshotđúng hoặc quyết disposition; không tự bịa để PASS.
4. Sau PM nhận completeness mới giao thiết kế bù/ghép; Gate1 DOING,Gate2 PARTIAL,Gate3/4 PARTIAL tạm dừng khóa cuối,Gate5–7 chưa mở. D04 OWNER ACCEPT11/09 chỉ nghĩa, không grant.

## Danh mục

- `dataset.json`, `dataset.sha256`: snapshot đầy đủ của dataset nhúng trong HTML; hash `c3fbd7447c961ba5b2b6fa619187face6c4cf25296945a37e90e7347e5acd32f`. Các view/heatmap lấy cùng nguồn.
- `heatmap.json`: snapshot12×9 và tổng, có hash dataset. Đã sửa lỗi Checkpoint0: hàng liên kết lệch family và nhãn Ban hành tách từ; mẫu số87/987 không đổi.
- `tools.json`: bảng tool lập trước cột Agent, source/caller/input/output/quyền/giới hạn.
- `continuation.json`: lời chỉ đạo Owner và hash phạm vi Checkpoint0.
- `historical-source-check.json`, `knowledge-search.json`: kết quả đọc currentHTTP và tìm KB; không tìm được9hàng nguồn cũ. Nguồn current trùng archive nên không lưu thêm bản HTML trùng.
- `source-restoration.json`:839file source/ảnh/native component được phục hồi tại kho cũ từ ZIP niêm phong khi link tham chiếu thiếu; không overwrite bản cũ hoặc đổi ZIP.
- `PM-REPORT.txt`: báo cáo dễ dán, cũng có trong HTML.
- `check-results.json`, `document-function-check.json`, `protected-check.json`: self-QA; không browser/runtime/independent acceptance. 23INV+Factory+ownership+8dòngKịchbản0 bất biến.
- `staged-update.json`, `ssot.diff`, `write-receipt.json`: vùng đổi, diff và biên nhận một lần ghi SSOT.
- `manifest.json`, `package-check.json`: kiểm hash file/bundle. ZIP chứa root HTML+SSOT và nguồn/ảnh theo đường tương đối; link đọc trỏ file giải nén.
- `document-harness.zip`: code dựng/kiểm tài liệu; không product code. Đọc khi cập nhật dataset/generator hoặc cần tái kiểm. Không tự chạy script handoff để ghi đè current revision.

Bằng chứng gốc Rev2/R1/Gate2 giữ nguyên thời điểm, cả các nhãn D04 pending đã cũ. Lab đã dọn; không mở lại trong công việc này. Workbook và nguồn thiết kế cũ không đổi vì chưa quyết contract/schema mới.

## Quy tắc cập nhật

Công việc thay field/control/UI/tool/contract phải cập nhật đúng step/UI refs, evidence, số tổng và heatmap trong cùng bàn giao. Những thay đổi material cần ghi amendment/phê duyệt đúng authority; không tự đổi87bước. Scope metadata/index correction không phải thêm/bớt nghiệp vụ. Chỉ ghi SSOT sau QA, có backup/hash chống ghi đè phiên khác.

Các file kiểm nguồn/gói bổ sung: `source-manifest.json` giữ180hash nguồn/ảnh; `code-inventory.json` kê code tài liệu; `package-plan.json` ghi phạm vi352file được gom trước bước niêm phong; `zip-verification.json` là biên nhận kiểm ZIP sau khi tạo, nằm ngoài ZIP để tránh vòng tự-hash. Link tải ZIP trong snapshotHTML chỉ dùng tại thư mục dự án gốc; người đã giải nén đã có ZIP, không cần tải lại.

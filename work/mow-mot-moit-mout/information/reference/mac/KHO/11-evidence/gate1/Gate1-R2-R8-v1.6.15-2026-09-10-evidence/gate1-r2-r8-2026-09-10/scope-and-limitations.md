# Frozen scope, method và giới hạn

- Freeze duy nhất: **41 candidates =40 URL +1 source group S01**, gồm U01/U05–U28 và một overlap R1 Canvas C40 để kiểm runtime. R1 đã PM nhận riêng:7 URL/vùng U04; không cộng R1 và R2–R8 thành 48 unique app vì có overlap.
- **41/41 đã có13 trường review, disposition, evidence và gap**. 39/40 URL được mở trình duyệt:37 có nội dung/redirect dùng để đánh giá; C36/C37 đi tới Directus login rồi lỗi404. C14 cố ý chỉ đọc source vì trang tự chạy `runAll` khi load. S01 chỉ là source group, không tự đặt URL.
- **Overflow app = 0, added after freeze = 0.** Ba drilldown Trigger/directus_fields/FieldStandards của C29, drawer/detail và edit handoff C02/C06/C09, Help C05/C12 là trạng thái phụ của candidate đã freeze. Chúng có URL/evidence riêng nhưng không app mới. Chưa duyệt hàng loạt category/knowledge document/registry detail khác; đó là record traversal ngoài câu hỏi role/gap, không phải bỏ sót app đã freeze.
- Discovery bounded dùng U/current nav/source pages. R1 còn lại không mở lại. Blueprints technical documents, thư mục/assets/CSS, deep document record links không được đưa thành business UI candidate. Source dependency tải sau freeze không làm thay candidate count.
- Source S01 freeze có hai tên path giả định chưa chính xác: `base/FormCustom.vue` thực tế là **base/FormCustom.ts**; `base/DirectusTable.vue` thực tế là **shared/DirectusTable.vue**. Resolve cùng component đã định danh, không thêm candidate; bản freeze/hash gốc không bị sửa. [Native source index](native-source-index.json), [dependencies](native-dependencies-index.json).
- C05 lần chụp đầu chỉ có header do JS chưa render xong; dùng `C05-ready`/`C05-help` và source MOT_DATA/MOT.complete cho kết luận. C36/C37 initial Loading không được lấy làm kết luận cuối; dùng ready screenshot/AX đã chuyển Directus login404.
- C21/C22 HTTP 200 chỉ trả alias meta-refresh; browser đi v3. Không gọi chúng là hai builder khác đang hoạt động. C23 chỉ card thẩm mỹ dù URL cũ có query đề xuất.
- Local Nuxt source không mặc nhiên source deployed: C31 category UI khác local file; C36/37 bị route về Directus; C33 có hai route files. Kết luận tách browser/SSR evidence, local asset và compatibility gap. Không kích hoạt endpoint mutation để chứng minh.
- “Reads/writes” là source analysis + browser read-only. Không click Save/Đúc/Done/Gửi/Approve/Test, không login/cấp quyền. NTGV sessionStorage có trạng thái mẫu sẵn; nguồn tạo local state trước lần đọc không xác định, không phải approval DB.
- Không cài runtime, không schema/product edit/deploy/upgrade, không formal Pilot/R9. Browser đọc có request GET/runtime client behavior thường; không gọi đó là complete production audit. Không HTTP submit command nào được chủ động thực hiện.
- Nuxt docs web tool có lỗi content-type markdown; đã đọc qua HTTP nguồn chính thức. SDK guessed monorepo path 404: dùng official release SDK 25.0.1; không kết luận SDK không tồn tại. Current-version query cũ `source-current.json` có lỗi SQL; PG version lấy file riêng `source-database.json`.
- Nguồn KB search metadata Foundation cũ, body v3.3/rev12 và Constitution v4.6.3/rev44 làm current evidence. Không sửa luật nguồn. Thông tin doanh thu Owner đã cung cấp không bị hỏi lại; OIG cần thêm đúng facts/phạm vi còn thiếu trong platform package.

Điều kiện READY là đủ evidence chọn vai trò và nén gap. Hai URL lỗi và C14 không mở không làm PM thiếu căn cứ chọn role: identity source còn Gate3/4, knowledge tree khác org tree, QA là expert UI harness. **Không kết luận các màn lỗi đã có khả năng hoạt động.** Gate1 chỉ DOING/SUBMITTED tới khi PM quyết.

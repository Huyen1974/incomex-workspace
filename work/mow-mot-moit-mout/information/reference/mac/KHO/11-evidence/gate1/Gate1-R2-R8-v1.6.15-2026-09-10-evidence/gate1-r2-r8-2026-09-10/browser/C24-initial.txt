Browser tab: 14, Title: "Guide — Cửa vào hệ UI 4 Mẹ (đọc trước tiên)", URL: "https://vps.incomexsaigoncorp.vn/ui-preview/mcp-writes/guide.html".
1 AXWebArea Guide — Cửa vào hệ UI 4 Mẹ (đọc trước tiên), URL: vps.incomexsaigoncorp.vn/ui-preview/mcp-writes/guide.html
	2 heading 📖 Guide — Cửa vào hệ UI "4 Mẹ", Value: 1
		3 text 📖 Guide — Cửa vào hệ UI "4 Mẹ"
	4 container
		5 text Đọc trang này TRƯỚC. Nó nói: hệ thống có gì, RANH GIỚI runtime (UI khai khuôn — không chạy), công cụ, luật, cách làm việc "sửa rẻ", DOT cần xây, và lộ trình. Mục tiêu cao nhất của hệ:  việc SỬA phải RẺ .
	6 text Dành cho: mọi Agent / AI / người mới tiếp quản
	7 heading 1 · Hệ thống này là gì, Value: 2
		8 text 1 · Hệ thống này là gì
	9 container whatis
		10 text Một hệ giao diện gồm  4 "Mẹ"  (MOW · MOT · MOIT · MOUT · Master) để dựng và vận hành quy trình doanh nghiệp:  MOW  thiết kế quy trình,  MOT  giao việc/làm việc,  MOIT  nhập liệu,  MOUT  báo cáo — cùng tầng  Master  tổng hợp + quản trị. Mỗi Mẹ có 1 trang "biến thành chạy được" (MOD*) để lắp khuôn nối DB.
		11 text Tinh thần:  "UI-side contract. Số liệu là YÊU CẦU UI đặt ra cho PG, không phải trạng thái PG hiện tại."  — làm để chạy thật, sửa phải rẻ.  (manifest v5.3 (+kestra_heads + directus_capabilities + integrity_contract + pre_dot_birth_blockers))
	12 heading ★ Ranh giới runtime — UI khai khuôn, KHÔNG chạy, Value: 2
		13 text ★ Ranh giới runtime — UI khai khuôn, KHÔNG chạy
	14 container rtb
		15 text UI 4 Mẹ là nơi KHAI KHUÔN, KHÔNG phải nơi CHẠY. Runtime đi qua Directus / DOT / Kestra.
		16 text ĐỌC: PG → Directus → Nuxt/UI
		17 text GHI: UI → DOT/gateway → validate/gate → ghi PG (hoặc tạo đề xuất chờ duyệt) → phát event
		18 text KESTRA: workflow đã duyệt → Kestra execution → tới bước người làm: tạo task MOT qua DOT → người làm xong: DOT ghi event/trạng thái → Kestra resume bước tiếp
		19 text PG
		20 text Nơi lưu SỰ THẬT nghiệp vụ (Source of Truth).
		21 text Directus
		22 text Lớp ĐỌC: API + quyền + row-filter + collection + metadata.
		23 text Nuxt/UI
		24 text Lớp RENDER + khai khuôn. Không giữ logic nghiệp vụ nặng.
		25 text DOT/gateway
		26 text Lớp GHI nghiệp vụ có kiểm soát: validate + gate (Đ0-G/Điều 32) + idempotency + phát event.
		27 text Kestra
		28 text Lớp ĐIỀU PHỐI quy trình/task dài ngày, quy mô lớn (lịch/chờ/nhánh/retry/pause-resume). CHƯA dựng — chỉ chừa đầu chờ.
		29 container
			30 text 4 luồng:  design-time (Người/Agent dùng UI để khai khuôn quy trình, task, form, report.)
			31 text approval-time (Thay đổi quan trọng → đề xuất, lưu vĩnh viễn, duyệt rồi mới publish (Đ0-G + Điều 32).)
			32 text runtime (Quy trình chạy, task sinh ra, việc chảy về MOT đúng người, đúng lúc.)
			33 text analytics-time (Mọi thao tác/sự kiện được ghi (event) để phân tích năng suất, cải tiến.)
		34 text luật
		35 text UI KHÔNG ghi nghiệp vụ trực tiếp vào PG.
		36 text luật
		37 text Đọc qua Directus; GHI nghiệp vụ qua DOT/gateway (không phải hai đường ngang nhau).
		38 text luật
		39 text Kestra gọi DOT, KHÔNG update bảng nghiệp vụ trực tiếp; mỗi lần chạy có correlation_id / workflow_run_id.
		40 text luật
		41 text MOT state (người làm: mở/đang làm/xong/sửa/chặn) TÁCH khỏi Kestra state (điều phối: waiting/running/paused/failed/resumed).
		42 text luật
		43 text Không phải task nào cũng cần Kestra — chỉ task sinh từ quy trình có chờ/nhánh/lịch/quy mô lớn.
		44 container
			45 text MODW:  MOW = nơi THIẾT KẾ & nhìn quy trình nghiệp vụ. MODW = lớp CON của MOW, biến thiết kế đó thành workflow/task CHẠY ĐƯỢC.
			46 text Task lắp từ:  field nhập · field hiển thị/báo cáo · trigger · người thực hiện · người/bộ phận nhận báo cáo · quan hệ 7 tầng · quan hệ quy trình/bước · ĐỐI TƯỢNG NGHIỆP VỤ cụ thể (học sinh/đơn hàng/hợp đồng/khách hàng)
	47 heading 2 · Bạn có những công cụ này, Value: 2
		48 text 2 · Bạn có những công cụ này
	49 container tools
		50 link Value: vps.incomexsaigoncorp.vn/ui-preview/mcp-writes/qa-runner.html, Description: 🚨 Bảng kiểm hồi quy + dò lệch Máy dò khói: chạy mọi hợp đồng đã chốt → đèn xanh/đỏ. Có suite "Đồng bộ 3 thực thể" DÒ LỆCH UI↔Bản đồ↔Ma trận + quét vi phạm UI→PG. Chạy TRƯỚC & SAU mỗi thay đổi. qa-runner.html
		51 link Value: vps.incomexsaigoncorp.vn/ui-preview/mcp-writes/he-thong-thiet-ke.html, Description: 🗺️ Bản đồ hợp đồng dữ liệu Hệ thống CÓ GÌ + cần tương tác bao nhiêu bảng/trạng thái/cột + ranh giới runtime. Render từ manifest này. he-thong-thiet-ke.html
		52 link Value: vps.incomexsaigoncorp.vn/ui-preview/mcp-writes/he-thong-dau-noi.html, Description: 🔌 Ma trận đấu nối UI↔PG Tình trạng từng bảng qua 4 nấc: khai báo → PG có bảng → đấu nối OK → đã test. Tự tính khoảng trống. he-thong-dau-noi.html
		53 link Value: vps.incomexsaigoncorp.vn/ui-preview/mcp-writes/guide.html, Description: 📖 Guide cho Agent Cửa vào: đọc đầu tiên để hiểu hệ thống, ranh giới runtime, công cụ, luật, cách làm việc mà không phá đồ cũ. guide.html
		54 link Value: vps.incomexsaigoncorp.vn/ui-preview/mcp-writes/eco-nav.js, Description: 🧭 Menu chung (eco-nav) 1 nguồn menu, tự đồng bộ mọi trang. Chữ ký __ECO_SIG + __ECO_APPS để QA so nhất quán/dò lệch. eco-nav.js
		55 link Description: 📋 Khuôn danh sách đã đúc Renderer dùng chung cho mọi Danh sách + Master hub. master-list.js, Value: vps.incomexsaigoncorp.vn/ui-preview/mcp-writes/master-list.js
		56 link Description: 💡 Component hướng dẫn Hướng dẫn đa phương tiện gắn vào builder/UI. guide.js, Value: vps.incomexsaigoncorp.vn/ui-preview/mcp-writes/guide.js
	57 container
		58 text ⚠ Trước & sau MỌI thay đổi: mở  qa-runner.html  (dò tính năng hỏng + dò lệch 3 thực thể). Tất cả xanh mới được nói "xong".
	59 heading 3 · Luật bất biến — phá là sai, Value: 2
		60 text 3 · Luật bất biến — phá là sai
	61 container laws
		62 text ADD-ONLY Chỉ thêm qua lớp phủ
		63 text Tính năng mới viết vào file *-enhance.js / trang mới. KHÔNG viết đè code cũ.
		64 text CORE-FREEZE Đóng băng lõi
		65 text Không sửa trực tiếp: mot-app-v1.js, mot-render-v1.js, master-list.js, eco-nav.js. Chỉ thêm hook chỉ-đọc, có ghi chú.
		66 text SINGLE-SRC Một nguồn, sửa 1 chỗ
		67 text Menu, hợp đồng, danh sách… mỗi thứ 1 nguồn duy nhất (eco-nav APPS, system-manifest, master-list). Cấm bản sao.
		68 text SELF-DECLARE UI tự khai báo binding
		69 text Trang UI khai window.UI_CONTRACT ngay tại chỗ (bảng nào, đọc/ghi). DOT-HARVEST gom về manifest; bộ dò lệch so khớp.
		70 text NO-DIRECT-PG UI không ghi thẳng PG
		71 text UI khai khuôn, KHÔNG chạy. ĐỌC qua Directus; GHI nghiệp vụ qua DOT/gateway (validate + gate + event). CẤM mô tả UI→PG là luồng chuẩn.
		72 text DECOUPLE Tách ghi giá trị ⟂ vẽ lại
		73 text Commit/persist KHÔNG kéo theo full re-render. Nguồn gốc lỗi nuốt-click.
		74 text NO-DEAD-BTN Không nút chết
		75 text Nút hành động luôn cho phản hồi: hoặc chạy, hoặc chỉ rõ còn thiếu gì. Cấm nút xám im lặng.
		76 text SOT-BIND Buộc đúng bảng SOT
		77 text Mỗi connector buộc đúng 1 bảng nguồn-sự-thật; thiếu → DOT tạo trước rồi mới nối.
		78 text D0-G Cổng khai sinh Đ0-G
		79 text Mọi entity mới phải có bản khai sinh (birth_registry) trước khi bind.
		80 text APPROVE Cổng duyệt Điều 32
		81 text Mọi thay đổi hệ thống qua cổng phê duyệt duy nhất (approval_requests).
		82 text PG-FIRST PG là sự thật
		83 text PG = nguồn sự thật; DOT = lớp thực thi (ghi/đọc/hàm/trigger); Nuxt chỉ render.
		84 text VERIFY Definition of Done
		85 text Mỗi thay đổi: chạy qa-runner trước & sau (gồm suite dò lệch); chỉ "xong" khi xanh hết + cái mới xanh.
		86 text QA-APPEND Kiểm mới = NỐI THÊM
		87 text Thêm phép kiểm QA bằng cách append QAC(...) vào qa-contracts.js — KHÔNG viết lại harness qa-runner. Cỗ máy đứng yên, hợp đồng cứ nối thêm.
		88 text QA-STRUCTURAL Kiểm phải TẤT ĐỊNH
		89 text Phép kiểm đọc dữ liệu cấu trúc (DOM/JSON/biến), CẤM "đoán chữ" heuristic → tránh đỏ giả (bài học vòng NO-DIRECT-PG).
		90 text APPEND-FIRST Ưu tiên nối thêm
		91 text Thay đổi nên là NỐI THÊM vào vùng append / file phụ, hạn chế ghi đè file lớn. File nhỏ + tách dữ liệu khỏi cỗ máy = sửa rẻ.
		92 text KEY-CONVENTION Quy ước khóa
		93 container
			94 text PK kỹ thuật id bất biến; cột [role_] _id ⇒ FK có role tới entity. code nghiệp vụ = unique, không dùng làm PK.
		95 text REL-DERIVED Quan hệ suy ra + tách logic/vật lý
		96 text FK suy từ quy ước, không khai tay (trừ ngoại lệ). Relation logic trỏ entity_code; DOT-REL resolve → collection+pk.
		97 text FK-PROMOTION FK cứng đi chậm
		98 text db_fk chỉ tạo khi entity đã khai sinh + không mơ hồ + kiểu khớp + orphan_count=0 + duyệt Điều 32 + có rollback. DOT-REL KHÔNG tự ALTER.
		99 text NO-ARRAY-FK Quan hệ cần query → link table
		100 container
			101 text n-n cần query/integrity dùng bảng trung gian  __ _link, KHÔNG nhét array ID. (Danh sách config nhẹ vẫn dùng json/array được.)
		102 text RECORD-BIND 2 tầng khóa: bảng vs bản ghi
		103 text Tầng bảng = ui_relation (thiết kế). Tầng bản ghi = giá trị FK thật + trường reference (chọn/suy bản ghi). Dữ liệu ăn theo bản ghi.
		104 text TABLE-FIRST Nhập liệu: BẢNG trước, CỘT sau
		105 text Mỗi trường nhập chốt target_collection (bảng/đối tượng) TRƯỚC rồi mới chọn cột từ bảng đó (hoặc cột bảng liên quan qua ui_relation + bản ghi đã khóa). MOT-nhập-liệu và MOIT/MODIT dùng CHUNG 1 engine ui_input_form (2 hình hài) — sửa 1 lần được cả 2.
		106 text INPUT-KIND Trường nhập = reference | value; nhập qua nhiều bước
		107 text Loại A reference = ghi khóa phụ (chọn bản ghi bảng khác, có thể multi→link table). Loại B value = ghi giá trị cột. Cùng 1 bản ghi nhập qua nhiều bước (chưa biết→create, đã biết→update; khóa phụ đã set thì không hỏi lại).
		108 text READ-CHEAP Đọc rẻ (nén định kỳ)
		109 text Thêm bằng APPEND (rẻ) làm NGUỒN THÔ rậm dần. Để ĐỌC rẻ: (a) đọc bản MERGED qua nút "Xuất JSON" trên Bản đồ; (b) ĐỊNH KỲ NÉN (squash) các khối append về định nghĩa gốc sạch — KHÔNG đổi hành vi, chạy QA phải vẫn xanh. Claude/Agent TỰ nén khi thấy nguồn rậm, không cần nhắc.
		110 text CONFORMANCE UI phải KHỚP hợp đồng — lệch phải LỘ
		111 text Đổi hợp đồng mà UI CHƯA theo thì hệ phải CẢNH BÁO, KHÔNG im lặng. Mỗi mặt UI khai window.UI_CONTRACT (điều nó THỰC HIỆN); bộ kiểm so với hợp đồng; requirement chưa đủ ở UI (partial/hypothesis) phải HIỆN RÕ như khoảng trống. Luồng soi: UI (thiết kế) → D (design) → danh sách hợp đồng.
		112 text REL-ROLE-KIND Khóa ngoại trỏ người phải khai BẢN CHẤT (actor/recipient/approver/subject)
		113 text Tránh gộp người-thực-hiện với người-nhận-báo-cáo — 2 bản chất khác, downstream khác.
		114 text PRE-BIRTH-GATE Không DOT-BIRTH khi pre_dot_birth_blockers chưa xong
		115 text Đúc bảng/dữ liệu thật khi hợp đồng chưa chốt = nợ đắt nhất.
	116 heading 4 · Cách làm việc "sửa rẻ" (vòng lặp an toàn), Value: 2
		117 text 4 · Cách làm việc "sửa rẻ" (vòng lặp an toàn)
	118 container flow
		119 text 1
		120 text Đọc nguồn
		121 container
			122 text Mở  system-manifest.js  (hợp đồng + luật + DOT) và  qa-runner.html . Hiểu trước khi sửa.
		123 text 2
		124 text Chạy bảng kiểm — nền
		125 container
			126 text Mở  qa-runner.html , xác nhận nền XANH (gồm dò lệch 3 thực thể). Đây là mốc so sánh.
		127 text 3
		128 text Chỉ THÊM, ưu tiên APPEND
		129 container
			130 text Tính năng mới → file  *-enhance.js /trang mới; dữ liệu → vùng APPEND cuối  system-manifest.js . KHÔNG sửa lõi. Trang UI mới → khai  window.UI_CONTRACT .
		131 text 4
		132 text Thêm hợp đồng kiểm = NỐI THÊM
		133 container
			134 text Mỗi điều "phải luôn đúng" → append 1 dòng  QAC(...)  vào  qa-contracts.js . KHÔNG viết lại harness qa-runner.
		135 text 5
		136 text Chạy lại bảng kiểm
		137 text Phải còn XANH HẾT + cái mới xanh. Có đỏ → sửa/hoàn tác trước khi đi tiếp. Kiểm phải TẤT ĐỊNH, cấm "đoán chữ".
		138 text 6
		139 text Mới được nói "xong"
		140 text Báo kèm kết quả bảng kiểm + soi UI thật. Không "tôi nghĩ là chạy được".
	141 heading 5 · Hợp đồng dữ liệu — UI cần gì ở PG, Value: 2
		142 text 5 · Hợp đồng dữ liệu — UI cần gì ở PG
	143 container
		144 text UI khai báo cần bao nhiêu bảng / trạng thái / cột. Đây là  yêu cầu , không phải PG hiện tại.
	145 container contractkpi
		146 text 23
		147 text Bảng
		148 text 228
		149 text Cột
		150 text 63
		151 text Lượt trạng thái
		152 text 21
		153 text Cần dựng
	154 container
		155 text Chi tiết + 3 ma trận + export: 
		156 link Description: Bản đồ hợp đồng →, Value: vps.incomexsaigoncorp.vn/ui-preview/mcp-writes/he-thong-thiet-ke.html
		157 text  · Tình trạng đấu nối: 
		158 link Description: Ma trận UI↔PG →, Value: vps.incomexsaigoncorp.vn/ui-preview/mcp-writes/he-thong-dau-noi.html
	159 heading ◆ Business Contract & Ma trận kiểm soát (Phase B/C), Value: 2
		160 text ◆ Business Contract & Ma trận kiểm soát (Phase B/C)
	161 container bcbox
		162 text Mỗi mặt UI nên khai đủ "Business Contract" (kèm window.UI_CONTRACT):
		163 text actor (ai dùng) in (đường vào) out (đường ra) states events[] (phát sự kiện gì) recipients (người nhận) needs_approval? (Điều 32) spawns_task? (sinh task MOT) spawns_report? mother_links[] (liên thông mẹ) needs_kestra? write_via (DOT/Directus, KHÔNG PG thẳng)
		164 text 7
		165 text Action→Event
		166 text 7
		167 text Mother-Link
		168 text 17
		169 text Yêu cầu gốc
		170 text 18
		171 text Surface boundary
		172 container
			173 text Đối tượng nghiệp vụ ( ui_business_object : học sinh/đơn hàng/hợp đồng) buộc task/form/report qua khóa; sự kiện ( ui_event ) cho analytics; đầu chờ Kestra (correlation_id, orchestration_state) tách khỏi state người. Chi tiết + 3 ma trận: 
			174 link Description: Bản đồ →, Value: vps.incomexsaigoncorp.vn/ui-preview/mcp-writes/he-thong-thiet-ke.html
	175 heading 6 · DOT cần xây (giao Agent thực thi), Value: 2
		176 text 6 · DOT cần xây (giao Agent thực thi)
	177 text Pipeline: HARVEST → PROBE → (BIRTH → WIRE → TEST) → GUARD
	178 container dots
		179 text DOT-HARVEST Thu hoạch hợp đồng
		180 text Quét khai báo window.UI_CONTRACT ở từng trang UI → cập nhật system-manifest (tables + bindings). Để "UI đổi thì hợp đồng tự đổi".
		181 text vào: Các trang UI (window.UI_CONTRACT) ra: system-manifest.js khi: Mỗi khi UI đổi / theo lịch cổng: Đổi quy chuẩn → Điều 32 DOT-PROBE Dò PG đối chiếu
		182 text So manifest với PG thật (bảng/cột/endpoint) → cập nhật binding-status (pg/wired/tested) + tính khoảng trống.
		183 text vào: manifest + PG schema ra: binding-status.js khi: Sau HARVEST / theo lịch cổng: Chỉ đọc PG DOT-BIRTH Khai sinh bảng thiếu
		184 text Bảng pg=pending: mở birth_registry (Đ0-G) + approval_requests (Điều 32); SAU duyệt mới tạo bảng/cột theo hợp đồng.
		185 text vào: tables pg=pending ra: bảng PG mới + bản khai sinh khi: Khi có khoảng trống PG cổng: Đ0-G + Điều 32 (bắt buộc) DOT-WIRE Đấu nối thật
		186 text Tạo đường đọc/ghi đúng chiều rw + record_binding + conditions + triggers. GHI nghiệp vụ qua DOT/gateway, KHÔNG ghi thẳng PG.
		187 text vào: tables pg=done, wired=pending ra: endpoint/binding hoạt động → wired=done khi: Sau BIRTH cổng: Điều 32 DOT-TEST Sinh & chạy kiểm thử
		188 text Với mỗi binding đã wired, sinh phép kiểm đọc/ghi, thêm vào qa-runner, chạy → tested=done.
		189 text vào: tables wired=done ra: hợp đồng QA + tested=done khi: Sau WIRE cổng: — DOT-GUARD Canh gác đồng bộ
		190 text Watchdog: UI/manifest đổi mà chưa HARVEST/PROBE lại → cảnh báo "khoảng trống mới"; giữ 3 thực thể luôn khớp thực tế.
		191 text vào: manifest + binding-status + lịch sử đổi ra: cảnh báo + kích hoạt HARVEST/PROBE khi: Liên tục/định kỳ cổng: — DOT-QA-HARVEST Gom hợp đồng kiểm → PG
		192 text Quét các QAC(...) trong qa-contracts.js + kết quả qa-runner → mirror sang bảng qa_contract_registry (PG). Giữ "điều đã hứa kiểm" ↔ "code kiểm" luôn khớp.
		193 text vào: qa-contracts.js (+ kết quả qa-runner) ra: qa_contract_registry khi: khi thêm/sửa kiểm / theo lịch cổng: — DOT-REL Dò & hòa giải KHÓA
		194 container
			195 text Discover (quét cột theo quy ước [role_] _id) → Classify (high/ambiguous/missing entity|role) → Validate (kiểu/orphan/required) → Propose (ghi ui_relation, status=proposed hoặc active validate_only). FK cứng: CHỈ tạo đề xuất approval_requests, KHÔNG tự ALTER TABLE.
		196 text vào: collections + ui_business_object + ui_relation_role ra: ui_relation (+ đề xuất db_fk) khi: khi schema đổi / theo lịch cổng: db_fk → Đ0-G + Điều 32
	197 container
		198 text Ta KHÔNG xây DOT — ta  đặc tả . Agent đọc mục này + hợp đồng + chuẩn binding để xây. Prompt:  agent-prompt-dau-noi.md .
	199 heading 7 · Chuẩn "một binding đầy đủ", Value: 2
		200 text 7 · Chuẩn "một binding đầy đủ"
	201 text Mỗi cặp (mặt UI × bảng) phải mô tả đủ 9 trường thì mới gọi là "đấu nối đầy đủ".
	202 container spec
		203 text surface
		204 text Mặt UI nào (mã trong surfaces)
		205 text table
		206 text Bảng SOT đích (mã trong tables)
		207 text op
		208 text read | write | upsert | delete
		209 text cols
		210 text Các cột chạm tới (con của tables[].cols)
		211 text record_binding
		212 text Định vị bản ghi: selector + mode — BẮT BUỘC cho write/upsert/update
		213 text conditions
		214 text Điều kiện hàm áp dụng (ref ui_condition)
		215 text triggers
		216 text Kích hoạt kèm theo (ref ui_trigger)
		217 text via_dot
		218 text DOT nào thực thi đấu nối này (GHI nghiệp vụ qua DOT, KHÔNG ghi thẳng PG)
		219 text gate
		220 text Cổng phải qua nếu tạo/đổi: Đ0-G (khai sinh) + Điều 32 (duyệt)
	221 container specwhen
		222 text Đầy đủ khi:  Mỗi mục rw trong tables[] có 1 binding khớp đủ 9 trường + bảng qua đủ 4 nấc (khai báo→PG có→đấu OK→test).
	223 heading 8 · Tự khai báo binding (để 3 thực thể tự đồng bộ), Value: 2
		224 text 8 · Tự khai báo binding (để 3 thực thể tự đồng bộ)
	225 text Mỗi trang UI TỰ KHAI BÁO binding của nó ngay tại chỗ. Nhờ đó "sửa UI → hợp đồng tự đổi", và lệch UI↔Bản đồ tự lộ ở bộ dò lệch.
	226 container sdex
		227 text // đặt gần code sinh ra binding, trong TỪNG trang UI
		228 text window.UI_CONTRACT = { surface:'modit', needs:[ {table:'ui_input_form', op:'write'}, {table:'ui_field_catalog', op:'read'}, {table:'ui_condition', op:'read'} ] };
	229 container sdby
		230 text Thu hoạch bởi:  DOT-HARVEST gom về system-manifest · bộ dò lệch trong qa-runner so khớp với hợp đồng.
	231 heading 9 · Đọc bằng máy (cho agent), Value: 2
		232 text 9 · Đọc bằng máy (cho agent)
	233 text Mọi thứ sinh ra từ MỘT nguồn. Đừng chép tay — đọc thẳng:
	234 container mach
		235 text // TỰ CẬP NHẬT từ SYS. Agent: đọc SYS.canonical + SYS.process TRƯỚC TIÊN (đường ghi + định nghĩa XONG).
		236 text // GHI UI vào ĐÚNG: /opt/incomex/docs/mcp-writes/ui-preview/ — nginx CHỈ phục vụ đây.
		237 text GET ./system-manifest.js
		238 text GET ./qa-contracts.js
		239 text GET ./eco-nav.js
		240 text GET ./entry-engine.js
		241 text GET ./modit-entry-enhance.js
		242 text GET ./guide.html
		243 text GET ./guide-extra.js
		244 text GET ./he-thong-thiet-ke.html
		245 text GET ./he-thong-dau-noi.html
		246 text GET ./entry-savemode.js
		247 text GET ./binding-status.js
		248 text GET ./web-state.json // mốc web-diff (fingerprint bytes-thật)
		249 text GET ./qa-runner.html // chạy MỌI hợp đồng + scanner chống-quên
		250 text // SYS.* (mọi phần hợp đồng — TỰ liệt kê, không bao giờ cũ):
		251 text action_events, backend_map, backend_map_meta, binding_spec, business_contract, canonical, capabilities, contract_gaps, data_entry_standard, defect_log, directus_capabilities, dot_pipeline, dots, entry_binding, entry_ops, entry_save, entry_stage, families, input_field_spec, integrity_contract, kestra_heads, key_model, laws, meta, mother_links, output_field_spec, pre_dot_birth_blockers, process, qa_model, relation_role_kinds, requirements, roadmap, runtime_boundary, scale_model, scope_code_spec, self_declare, surface_boundary, surfaces, sync_model, tables, tools
		252 text // Scanner chống-quên (qa-runner): Path-integrity + v2, Official-UI, Family, Capabilities, Web-diff, dò-lệch-3-thực-thể, conformance.
		253 text // UI khai khuôn — ĐỌC qua Directus, GHI nghiệp vụ qua DOT. KHÔNG ghi thẳng PG.
		254 text // Trang UI mới: MODULE tự đẩy window.UI_CONTRACT.capabilities (tag) ⇒ tự vào family.
	255 heading 10 · Lộ trình — đã chốt, CHƯA làm, Value: 2
		256 text 10 · Lộ trình — đã chốt, CHƯA làm
	257 container roadmap
		258 text Ghi ở đây để KHÔNG bỏ sót. Làm theo phase, mỗi phase chạy qa-runner xanh mới sang phase sau.
		259 text ▶ A-mini xong + nâng cấp công cụ (qa-contracts append-only, data_scope đồng bộ). Kế: contract mẫu học sinh → phase B khi cần.
		260 text REQ-RECORD-BIND
		261 container
			262 text MOIT/MODIT  — Input buộc vào BẢN GHI cụ thể (record_binding) + sơ đồ KHÓA CHÍNH/KHÓA PHỤ (PK/FK). Thiếu FK → nhập liệu rối loạn.
		263 text REQ-DATA-SCOPE
		264 container
			265 text MOUT/MODUT  — Báo cáo phải có MIỀN DỮ LIỆU (data_scope): theo thời gian (tháng/tuần/A→B) hoặc điều kiện (vd nữ nhập học tuần trước). 2 trường: scope_text (mô tả) + scope_code (mã đọc/nguồn).
		266 text REQ-BUSINESS-OBJECT
		267 container
			268 text MOW/MOT  — Task/node gắn ĐỐI TƯỢNG NGHIỆP VỤ cụ thể (học sinh/đơn hàng/hợp đồng).
		269 text REQ-EVENTS
		270 container
			271 text Chung  — Mỗi action phát EVENT (analytics-time) → phân tích năng suất. Ánh xạ event_type_registry ở backend.
		272 text PLAN-B
		273 container
			274 text —  — Phase B: mở rộng hợp đồng (business contract + business_object + data_scope + record_binding/FK + đầu chờ Kestra).
		275 text PLAN-C
		276 container
			277 text —  — Phase C: ma trận RENDER-từ-manifest (Action→Event, Mother-Link, Requirement-Coverage, Directus/Kestra boundary).
		278 text PLAN-SAMPLE
		279 container
			280 text —  — Ngay sau A-mini: 1 contract MẪU duy nhất — task "Điền thông tin liên lạc của học sinh" — chạy thông rồi mới nhân rộng.
		281 text PLAN-KEYS
		282 container
			283 text Chung  — ĐÃ THIẾT KẾ (design-only, sau hội đồng AI): KEY-CONVENTION/REL-DERIVED/FK-PROMOTION/NO-ARRAY-FK/RECORD-BIND + ui_relation + ui_relation_role + DOT-REL + 2 tầng khóa. CHỜ Agent dựng PG (DOT-REL/BIRTH/WIRE) khi có lệnh.
		284 text PLAN-ENTRY
		285 container
			286 text MOT+MOIT  — LÀM LẠI 2 khu nhập liệu (1 engine ui_input_form, 2 hình hài): chọn BẢNG→CỘT (table-first); mỗi trường phân loại reference(khóa phụ, có multi/tick→link table) vs value(gõ/checkbox); hỗ trợ nhập NHIỀU BƯỚC (create bản ghi mới vs update bản ghi đã biết). Bỏ "bảng mặc định" cũ ở MOT.
		287 text PLAN-CONFORMANCE
		288 container
			289 text công cụ  — Đóng lỗ hổng kiểm: mỗi builder khai window.UI_CONTRACT (điều nó thực hiện) → QA so với hợp đồng → UI lệch phải ĐỎ/HIỆN. Hiện bộ dò lệch mới kiểm artifact↔artifact, CHƯA kiểm UI-hành-vi↔hợp-đồng.
	290 heading ♻ SỬA RẺ & Sổ khái niệm, Value: 2
		291 text ♻ SỬA RẺ & Sổ khái niệm
	292 container
		293 text Sắp tạo/sửa UI? Đọc 
		294 link Description: SUA-RE-README.md, Value: vps.incomexsaigoncorp.vn/ui-preview/mcp-writes/SUA-RE-README.md
		295 text  —  hướng dẫn vận hành cho agent, đọc TRƯỚC KHI tạo/sửa UI . Vòng  KHAI→RÀ→CHỐT→ÁP : khai khái niệm vào Sổ một lần, máy gom nhãn khắp trang, người chốt ca khó, sổ đổi thì trang tự đổi. Sổ đang ở chế độ  SHADOW .
	296 text 6 bước
	297 container
		298 text ①  KHAI-UI  — dựng UI như thường.
		299 text ②  KÊ-UI  — mở  ui-contracts.js , thêm object surface theo schema (mỗi đầu mối nghiệp vụ = 1 item;  purpose  bằng lời +  location.file  BẮT BUỘC).
		300 text ③  KHỬ-TRÙNG  — nhãn cùng nghĩa trong CÙNG UI → cùng  duplicate_group .
		301 text ④  KHỚP-SỔ  — đối chiếu  CONCEPTS : matched / candidate (kèm near_concepts) / review (họ dễ nhầm). KHÔNG tự tạo thẻ mới trong sổ.
		302 text ⑤  CHỐT  — candidate/review hiện ở "Hàng chờ chốt"; Owner duyệt RỒI mới ghi sổ + đổi item sang matched + commit tường minh.
		303 text ⑥  ÁP+KIỂM  —  node verify-contracts.js  phải in OK; mở trang quy trình xác nhận render; qa-runner nếu đụng file lõi.
	304 container
		305 text Luật nghiệm thu:  UI mới hoặc sửa lớn mà KHÔNG nộp bảng kê ( ui-contracts.js ) = CHƯA nghiệm thu.
	306 text Công cụ
	307 container
		308 link Description: SUA-RE-README.md, Value: vps.incomexsaigoncorp.vn/ui-preview/mcp-writes/SUA-RE-README.md
		309 text  — hướng dẫn vận hành cho agent (đọc TRƯỚC KHI tạo/sửa UI) · 
		310 link Description: quy-trinh-sua-re.html, Value: vps.incomexsaigoncorp.vn/ui-preview/mcp-writes/quy-trinh-sua-re.html
		311 text  — nhìn tổng thể (tra Sổ, hàng chờ chốt, bảng kê) · kiểm bảng kê:  node verify-contracts.js
	312 container 4 Mẹ + Master
		313 text 4 MẸ
		314 button MOW ▾ Quy trình
			315 text MOW
			316 text ▾
			317 text Quy trình
		318 button MOT ▾ Công việc
			319 text MOT
			320 text ▾
			321 text Công việc
		322 button MOIT ▾ Nhập liệu
			323 text MOIT
			324 text ▾
			325 text Nhập liệu
		326 button MOUT ▾ Báo cáo
			327 text MOUT
			328 text ▾
			329 text Báo cáo
		330 button Master ▾ Tổng 4 Mẹ
			331 text Master
			332 text ▾
			333 text Tổng 4 Mẹ

The focused UI element is 1 AXWebArea Guide — Cửa vào hệ UI 4 Mẹ (đọc trước tiên), URL: vps.incomexsaigoncorp.vn/ui-preview/mcp-writes/guide.html
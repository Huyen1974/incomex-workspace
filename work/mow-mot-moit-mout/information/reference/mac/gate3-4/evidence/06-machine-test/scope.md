# Máy tự kiểm sau compile

Một lệnh compile-and-test, đọc Draft/refs hiện hành, compile readiness và ghi evidence TEST riêng bằng nonadmin machine-tester. Ca dữ liệu chạy qua cổng ghi; 59/60/61 và output thiếu chạy qua pg-boss/guard/Flow thật. Negative missing executor/capability/output-contract chạy cùng compiler trên snapshot bị thay đổi, không gọi đó là PG lifecycle enforcement. Staleness phải được kiểm ở bước7 trước Publish/Activate, không chỉ so hai số revision.

Source expected: intent PM, typed input profile, C05/INV08, C03 idempotency/CAS, C07 output before ACK. Fixture/contract-derived templates; không lấy output implementation làm expected. Không O1 hoặc Gate5 PASS.

# Nguồn phục vụ proof, không mở lại platform adoption

- https://docs.directus.io/app/flows/triggers — blocking filter có thể chặn transaction; phải kiểm nested operation atomicity trong exact image, không suy từ tên Flow.
- https://docs.directus.io/app/flows/operations — native condition/read/create/update operation; quyền lấy từ accountability.
- https://docs.directus.io/reference/system/permissions — native permission/filter/validation.
- https://learn.chatgpt.com/docs/non-interactive-mode — Codex exec --json thu command/tool events; --ephemeral không reuse rollout; authentication CLI có sẵn. Đã đối chiếu local CLI help. Không đổi model do người dùng chưa yêu cầu.

Trang được mở/tra ngày 11/09/2026; đây là căn cứ chọn phép thử, không thay thế runtime proof.

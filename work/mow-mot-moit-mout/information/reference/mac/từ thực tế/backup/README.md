# Bản trước khi chỉnh cách nhìn

`từ thực tế đã làm.before-matrix-views.html` là bản nguyên ngày 15/09/2026, chỉ để khôi phục. File làm việc ở thư mục cha; xem README ở đó để biết trạng thái và phần còn mở.

`từ thực tế đã làm.before-daily-matrix.html`: bản ngay trước khi khôi phục cây 8 bước làm mặc định và thêm danh mục tất cả bảng theo Owner.

`từ thực tế đã làm.before-approved-ui-audit.html`: bản nguyên trước khi rà 12 nguồn UI được duyệt và điền thêm mục kiểm ngày 15/09/2026. Chỉ khôi phục khi cần, không phải hiện trạng đang dùng.

`từ thực tế đã làm.before-two-review-layers.html`: bản trước khi tách cách đọc thành lớp có UI và lớp rà nhãn/mục; số thiếu trong bản này chưa tách lớp, không dùng làm hiện trạng.

`từ thực tế đã làm.before-hierarchy-indent.html`: bản trước sửa thụt lề/phân biệt hàng mẹ-con ngày 16/09/2026. Chỉ dùng để khôi phục.

`từ thực tế đã làm.before-parent-first-review.html`: trước khi cập nhật nguyên tắc Master list/Canvas-Kanban và đặt bảng gán mẫu cha làm trọng tâm trong UI Master. Các nhận định thiếu UI cha ở bản này có phần đã bị thay thế.

`từ thực tế đã làm.before-shared-canvas.html`: trước khi ghi quyết định dùng Canvas chung cho MOIT/MOUT/FIELD, ngày 16/09/2026.

`từ thực tế đã làm.before-kanban-list-sync.html`: trước đồng bộ hai view và tách MOIT/MOUT cùng T0,5.
`cau-truc-he-thong.before-kanban-list-sync.html`: bản nguồn `/Users/nmhuyen/Desktop/quy trình/cấu trúc hệ thống.html` trước đồng bộ tên; chỉ để phục hồi.

`từ thực tế đã làm.before-master-list-entry.html`: trước đồng bộ Master list là đầu mối đối tượng và thông tin kiểm tra/khai báo/config.
`cau-truc-he-thong.before-master-list-entry.html`: bản cấu trúc hệ thống trước bổ sung cùng định nghĩa. Chỉ để khôi phục.

`từ thực tế đã làm.before-mout-ui-status-sync.html`: trước đồng bộ trạng thái UI tạo MOUT đã có. Chỉ để phục hồi, không là bản hiện hành.

`từ thực tế đã làm.before-simple-ui-review.html`: trước thu gọn rà UI theo Owner; chỉ để phục hồi.

- `từ thực tế đã làm.before-child-ids-search.html`: trước khi thêm mã UI con và khung tìm kiếm UI-029 ngày 16/09/2026; chỉ dùng đối chiếu/khôi phục, không là bản chuẩn.

- `từ thực tế đã làm.before-area-ui-list.html`: trước khi gộp bảng UI con vào UI Master và chia khu vực (UI-B02); bản khôi phục, không là nguồn đang dùng.

- `từ thực tế đã làm.before-parent-standards.html`: trước khi thêm gạch đầu dòng chuẩn mẫu cha, 16/09/2026; chỉ để khôi phục.

- `từ thực tế đã làm.before-moit-mout-children.html`: trước cập nhật năm UI con MOIT/MOUT, UI-B03; chỉ dùng khôi phục.

- `before-t0-field-declaration-20260916.html`: trước đồng bộ T0,5/T0 và UI-018; chỉ phục hồi, không phải nguồn hiện hành.

- `before-ui-green-20260916.html`: trước UI-B05, dùng phục hồi; không phải nguồn hiện hành.

- `before-search-parent-green-20260916.html`: trước sửa nhãn Tìm tại bảng mẫu cha, chỉ dùng phục hồi.

- `system-before-gate1-paths-20260916.html` và `audit-before-gate1-paths-link-20260916.html`: trước bổ sung bản ghép đường người Gate 1; chỉ dùng phục hồi.

- `before-process-tab-20260916.html`: trước đưa UI Master lên đầu và thêm tab Quy trình; chỉ phục hồi.

# COLLAB — vps-clean-20-9-26

## 0. MỤC TIÊU/NHIỆM VỤ USER — BẮT BUỘC ĐỌC TRƯỚC
### Vòng 2 · mở lại 2026-09-23 theo P18
- Mục tiêu (nguyên văn lời Owner 23/09, D11): "Điều quan trọng là toàn bộ các đầu dò của VPS đã được bịt, dữ liệu gì không cần để trên VPS thì [chuyển] xuống GD là ok."
- Phạm vi vòng 2 (diễn giải kỹ thuật, không thêm mục tiêu): CHỈ đặt trần cho image Docker (~10,7GiB) + build cache (~0,82GiB) — nguồn không phải nghiệp vụ còn tăng theo mỗi lần build (P18); không mở lại phần đã xong. Điều kiện an toàn đi kèm (không phải mục tiêu mới, P19): bật `live-restore` trước mọi thay đổi Docker; nếu cần nạp cấu hình GC thì restart dockerd tối đa 1 lần có kiểm soát, container không khởi động lại.
- Tiêu chí xong vòng 2: cả hai có trần tính được thành số bằng cơ chế tự động an toàn (không `docker buildx/builder/system prune/image prune`) và có hiệu lực ngay, có số đo trước/sau, health không đổi.
- Xác nhận User: **ĐÃ XÁC NHẬN** (nguyên văn lời User 23/09 tại D11; Owner giao "rà soát thêm và điều hành tiếp" điểm P18 ngày 23/09)

### Vòng 1 (lịch sử, đã xong)
- Mục tiêu: dọn VPS an toàn, không làm hỏng cơ chế hiện tại; tìm nguyên nhân ổ đĩa phình nhanh và đề xuất/xử lý cách chặn tăng trưởng để có chỗ cho công việc tiếp theo (Owner nêu: cài thêm Graph DB).
- Nhiệm vụ/phạm vi: khảo sát trước, không mutation production ở vòng R1; Claude đề xuất, GPT/Codex kiểm chéo kỹ trước khi Owner duyệt bất kỳ dọn/xoá thật nào. Theo D02: phân biệt dữ liệu nghiệp vụ / runtime đang cần / phải giữ dài (đưa ra ngoài VPS) / tái tạo được (đặt TTL rồi xoá tại chỗ, không mang sang Drive).
- Tiêu chí xong: xác định được nguồn tăng dung lượng với bằng chứng; MỌI nguồn không phải nghiệp vụ đều bounded (có trần local tính được, hoặc đưa ra ngoài có hạn giữ, hoặc tự xoá theo TTL) — còn nguồn tăng tuyến tính là chưa đạt dù tổng nhỏ; phương án dọn + khoá vòi được kiểm chéo; mọi mutation chỉ sau đúng cổng duyệt và có đường rollback/cứu trước xoá.
- Bổ sung phạm vi 2026-09-23 (lời Owner trong phiên Host): "đổi giờ VPS từ giờ Đức sang GMT+7 (Asia/Ho_Chi_Minh), GIỮ NGUYÊN thời điểm chạy thật của mọi lịch" (D09) — thuộc lượt khép việc R4b.
- Xác nhận User: **ĐÃ XÁC NHẬN** — Owner giao trực tiếp 2026-09-20 (D01) và bổ sung chính sách storage 2026-09-21 (D02); Host đã đưa D02 vào `PROMPT.md` và `view.html` (T1–T6); phạm vi múi giờ do chính Owner nêu 2026-09-23 (D09).

Host: Claude Chat · Host_ID: CLAUDE-VPSC-260920-A · mở việc theo lệnh Owner 2026-09-20
HTML chính: `view.html`

## Dòng hiện hành
VPSC | Dọn đĩa VPS + khoá vòi rò | vòng 2 (P18) · R5 DỪNG ở B5 | R5b: B5 + KEEP_SET v2 chế độ thử · PROMPT đã soạn | NEXT: GPT review R5b → Owner RUN ~06:45 VN → Host duyệt kế hoạch xoá → R5c → Đóng | BLOCK: —

- PROMPT hiện hành: `PROMPT.md` · RUN_ID `VPSC-R5B-20260924-01` · tiếp nối R5 theo P25: (B5) restart dockerd 1 lần có kiểm soát nạp trần build cache 5GiB; (C') KEEP_SET v2 hữu hạn (ACTIVE_ROLLBACK_SET + ân hạn 30 ngày) cài ở chế độ thử `L_XOA=0` + kế hoạch xoá chờ Host duyệt · CÓ MUTATION, KHÔNG xoá image. Giấy phép: dòng R5b dưới.
- (lịch sử R5) PROMPT: RUN_ID `VPSC-R5-20260923-01` · vòng 2: (A) bật live-restore qua reload; (B) chứng minh/đặt trần build cache không dùng lệnh builder; (C) luật giữ image (l) hằng tuần bằng `docker image rm` không `-f` · CÓ MUTATION. Giấy phép: xem dòng R5 ngay dưới.
- (lịch sử R5, đã dùng) **Giấy phép R5:** PROMPT_SHA = `e798203ec49a3f231c474aafcd8f9f49aa0bca82` (áp P19–P24; các bản trước hết hiệu lực) · **OWNER_APPROVED@e798203ec49a3f231c474aafcd8f9f49aa0bca82** (D08 uỷ quyền + lệnh Owner 23/09 "rà soát thêm và điều hành tiếp" P18) · **Host READY@e798203ec49a3f231c474aafcd8f9f49aa0bca82** — preflight 23/09 ~02:41Z (Host tự gọi): đĩa 45,0%, trống ~52,7GiB; 6 container lõi healthy, StartedAt không đổi từ 22/09 21:30Z; HTTP 200; failed chỉ `cloud-init` + `systemd-networkd-wait-online`; cổng NO_CONCURRENT agent tự kiểm lại. **RUN chỉ hợp lệ khi có thêm `GPT REVIEWED@` cùng SHA** (lượt có xoá image).
- (lịch sử R5) **GPT REVIEWED@e798203ec49a3f231c474aafcd8f9f49aa0bca82 · ACCEPT** — 2026-09-23 · đã thẩm định toàn bộ PROMPT R5 A→E sau P19–P24; diff cuối `a60e9bd→e798203` chỉ áp 3 điểm P24; A fail → không mutation B/C và R5 DỪNG; B5 có cửa sổ restart theo trạng thái job thật + rollback; C dùng KEEP_SET đầy đủ (container + reference direct/indirect đã resolve + 2 ImageID rollback), unresolved → DỪNG, `<none>` ngoài KEEP_SET mới được xoá; tiêu chí XONG khớp A0. Runtime read-only trước review: disk ~45,2%, 6 container lõi healthy, HTTP 200, failed chỉ `cloud-init` + `systemd-networkd-wait-online`. **Đủ điều kiện Owner RUN R5 theo đúng SHA này; không mở thêm scope.**
- **Lệnh RUN R5:** `WS work/vps-clean-20-9-26 · Agent · RUN VPSC-R5-20260923-01 · đọc AGENTS.md → work/vps-clean-20-9-26/COLLAB.md → work/vps-clean-20-9-26/PROMPT.md · READY@e798203ec49a3f231c474aafcd8f9f49aa0bca82`
- (lịch sử R4b) PROMPT: RUN_ID `VPSC-R4B-20260923-01` · khép việc: (1) dọn dấu vết SEC-01 sau khi xác nhận chìa cũ đã chết; (2) báo image hằng ngày chỉ bằng lệnh xem (luật (k)); (3) sửa dịch vụ failed (kuma-push: chạy lại, cấm tắt; config-drift theo H1) + chẩn đoán `unless-stopped`; (4) đổi múi giờ sang Asia/Ho_Chi_Minh giữ nguyên thời điểm chạy thật · CÓ MUTATION.
- **PROMPT_SHA = 0a08377d915f0dae0417e63175728e0d3de60282**.
- **OWNER_APPROVED@0a08377d915f0dae0417e63175728e0d3de60282** (D08 uỷ quyền + lệnh Owner 23/09 "đọc kỹ và tiếp tục" giao Host soạn R4b gồm đúng 4 phần trên) · **Host READY@0a08377d915f0dae0417e63175728e0d3de60282** — preflight 22/09 ~22:15Z (Host tự gọi): đĩa 44,9%, trống ~52,8GiB; 6 container lõi healthy, StartedAt không đổi từ lần khôi phục 21:29–21:30Z (không có recreate/deploy chen); web + agent-data HTTP 200; failed: cloud-init, systemd-networkd-wait-online, incomex-config-drift-check, incomex-kuma-push; root `workspace` hết RECOVERY_REQUIRED (Codex xử lý 23/09), cả `workspace_*` và `fs_*` đọc được HEAD mới; việc khác trong root COLLAB (GSM-A1 chỉ đọc, mcp-token-argv, HJW design) không chạy chen trong cửa sổ R4b; Executor Claude Code CLI Mac → SSH (đã chạy R3/R4); Write_Path `workspace_*`, dự phòng `fs_*`.
- **Host đối chiếu mã trước READY (23/09):** crontab root có dòng `CRON_TZ=` (code-backup theo giờ VN, Qdrant theo UTC) nhưng R3 ghi Qdrant chạy "03:00 giờ máy" ⇒ PROMPT 4.2 bắt xác minh cron có tôn trọng CRON_TZ bằng log thật trước khi quy đổi; `scripts/vps-retention.sh` có cổng giờ `date +%H` bên trong ⇒ đổi sang UTC cố định; config-drift fail do H1 (baseline), không do Docker sập; các script phái cử dùng múi +7 cứng nên không bị ảnh hưởng. **Sửa 23/09 theo câu hỏi Owner:** `incomex-kuma-push.service` là bộ nạp token Kuma từ `kuma.db` (không phải bản trùng cron R3) ⇒ PROMPT 3.2 chỉ chạy lại, cấm tắt; thêm §4b kiểm cuối 2 tính năng sống còn (Kuma → Telegram, backup Drive) — hỏng thì lùi + DỪNG. PROMPT đổi từ `7dd82f7` sang `fbb28bb` rồi `0a08377` (áp P17), READY cũ vô hiệu.
- R4 (lịch sử): PROMPT_SHA `f4d496e7393659be03635a33edb575d7d717c06b` · OWNER_APPROVED + READY đã dùng · DỪNG ở A3 (BAO-CAO R4, `3884ba5`); thay bằng R4b.
- **Host kiểm R3 (22/09 ~11:20Z):** đã đọc mục R3 trong `BAO-CAO.md`; tự đo 45%, trống 53GB (khớp 52,784GiB agent báo); 12 container healthy. R3 đạt: vượt 45GiB, Drive có hạn giữ, cảnh báo đĩa qua Telegram hoạt động lại, mọi vòi trong bảng §3e có trần. Còn: gỡ quyền client chung (Owner) + dọn dấu vết, build cache chưa có trần, image chờ R03.
- R3 (lịch sử): **PROMPT_SHA `dd216dff830a426199bd57491bb83a114490c127`** · OWNER_APPROVED + READY đã dùng · KQ XONG.
- **OWNER_APPROVED@dd216dff830a426199bd57491bb83a114490c127** (D08) · **Host READY@dd216dff830a426199bd57491bb83a114490c127** — preflight 22/09 ~10:30Z: đĩa 55%, trống 43,3GiB; 12 container healthy; Phase 0/1.1/1.2 đã xác minh qua báo cáo `63c8d68`; Executor Claude Code CLI trên Mac → SSH (đã chạy phiên 1); Write_Path `workspace_*`. GPT REVIEWED@f249b90 vẫn là cơ sở cho Phase 1–3; phần thêm (Phase 4–5, chế độ chạy) do Owner uỷ quyền trực tiếp (A6: OWNER_APPROVED) — GPT có thể mở P sau, không chặn.
- R3 phiên 1 (lịch sử): **PROMPT_SHA = f249b90f9a5d84bce5295f22b7628fd716fe5ba3** (sau P16). Bản `fb182de` hết hiệu lực — OWNER_APPROVED@fb182de chuyển sang SHA mới vì chỉ đổi Phase 0 theo P16, không mở rộng phạm vi Owner đã gật (Q06). Cần đủ ba: `GPT REVIEWED@` + `OWNER_APPROVED@` + Host `READY@` (preflight gồm NO_CONCURRENT_VPS_MUTATION).
- **OWNER_APPROVED@f249b90f9a5d84bce5295f22b7628fd716fe5ba3** — chuyển từ OWNER_APPROVED@fb182de (Q06 "cần chốt dứt điểm"); bản mới chỉ đổi Phase 0 theo P16, phạm vi không rộng hơn.
- **Host READY@f249b90f9a5d84bce5295f22b7628fd716fe5ba3** — preflight (DROOT04, 22/09): commit cuối chạm `PROMPT.md` = `f249b90`; GPT REVIEWED + OWNER_APPROVED trùng mã; đĩa 53/96GB, 55%, trống 44GB (qua đêm không phình lại); 12 container healthy; NO_CONCURRENT_VPS_MUTATION đạt (agent-data + claude-mcp không bị tạo lại trong ~15 giờ); Executor Claude Code CLI trên Mac → SSH (đã chạy R1/R2); Write_Path `workspace_*` (đã ghi ở R1/R2/V2). Điều kiện vận hành: Owner ngồi máy Phase 0; không giao HVU/việc khác mutation VPS trong lúc R3 chạy.
- **GPT REVIEWED@f249b90f9a5d84bce5295f22b7628fd716fe5ba3 · ACCEPT** — đã đối chiếu diff `fb182de→f249b90`: chỉ Phase 0/P16 thay đổi; Phase 1–3 giữ nguyên. Phase 0 mới cấm auto-revoke/shared-client reauth, chỉ cho Drive phases khi `SEC-01=MIGRATED`; các gate fail-closed, NO_CONCURRENT_VPS_MUTATION, N9/N11 rescue-before-delete và V3 hậu kiểm vẫn đầy đủ. Không mở P mới.
- **OWNER_APPROVED@fb182de5e79ac1ecda0ec41ec279ff72f3e2068c** — Owner 21/09: "Cần chốt dứt điểm việc clean này, bịt hết các đầu dò và có chế độ tự xoá hợp lý … toàn rác làm phình" (gật Q06).
- **Host đối soát 21/09 ~11:40Z (chỉ đọc, theo đề nghị GPT):** đoạn log Owner chuyển (B4/K1/K2 với commit `b97e5d3`/`3180326`/`0dc9379`) là log R2 phiên 2, KHÔNG phải R3. Bằng chứng: `qdrant-backup.sh` sửa lần cuối 10:19Z, `dung-va-trien-khai.sh` 10:20Z, `vps-retention.sh` 10:21Z (đúng cửa sổ R2 phiên 2), `backup-to-gdrive.sh` vẫn 2026-08-02; `BAO-CAO.md` chưa có mục R3; `VPSC.5b` vẫn DRAFT. **R3 chưa bắt đầu, không có mutation ngoài quy trình, không cần rollback.**
- V2 (lịch sử): PROMPT_SHA `abe38f1992a61dedd9f3a9a664a63b6356b19cc4` · OWNER_APPROVED + READY đã dùng · KQ XONG PASS 3 / REVISE 3 / BLOCK 0.
- **OWNER_APPROVED@abe38f1992a61dedd9f3a9a664a63b6356b19cc4** (D06: Host chỉ đạo Codex trực tiếp; Owner 21/09 "điều hành tiếp") · **Host READY@abe38f1992a61dedd9f3a9a664a63b6356b19cc4** — preflight (21/09 ~11:10Z): đĩa 53/96GB, 55%, trống 44GB; 12 container healthy, agent-data về lại image `agent-data-r03:20260920-finalclose`; Executor Codex Desktop qua SSH (đã chạy V1/V1b); Write_Path `workspace_*` (R2 đã ghi được sau khi HVU-B3 rollback); R03 CLIENT-ONLY; V2 chỉ đọc.
- R2 (lịch sử): PROMPT_SHA `f701fc5ca09041d47752cc7c5bca46ab290e8043` · GPT REVIEWED + OWNER_APPROVED + READY đã dùng · KQ XONG. **Host tự đo sau R2 (21/09 ~11:10Z):** 91% → 55%, trống 9,4 → 44GB. Ghi nhận: HVU-B3 đã chạy chen 10:20–10:25Z trái điều kiện vận hành của READY; agent R2 xử lý đúng (đợi ổn định, kiểm health rồi mới làm B2/B3).
- **GPT REVIEWED@f701fc5ca09041d47752cc7c5bca46ab290e8043 · ACCEPT** — đã kiểm A1–A4 + B1–B4 + K1–K2, gate dừng/health, phạm vi HOLD và đường deploy Nuxt thực tế; không mở P mới. R2 đủ để Owner duyệt mutation.
- **OWNER_APPROVED@f701fc5ca09041d47752cc7c5bca46ab290e8043** — Owner 21/09: "giao cho Claude Code CLI prompt bạn vừa soạn" (gật Q05).
- **Host READY@f701fc5ca09041d47752cc7c5bca46ab290e8043** — preflight (DROOT04, 21/09 ~09:20Z): commit cuối chạm `PROMPT.md` = `f701fc5`; GPT REVIEWED + OWNER_APPROVED trùng mã; đĩa 87/96GB, 91%, trống 9,4GB (> sàn 3GiB); 12 container healthy; Executor_Surface Claude Code CLI trên VPS (đã chạy R1); Write_Path `workspace_*` (đã ghi ở R1/V1/V1b); R03 CLIENT-ONLY, R2 không đụng image. Điều kiện vận hành: KHÔNG chạy song song HVU-B3 (sửa cổng Workspace = Write_Path của R2, và có thể build image trên đĩa 91%) — R2 trước, B3 sau.
- **R2 đang chạy (Host ghi 21/09 ~09:50Z):** đợt A XONG — agent báo thu hồi 6,846GiB; Host tự đo: 91% → 84%, trống 9,4 → 17GB, 12 container healthy, Qdrant 20181 điểm green. B1 PASS. **B2 bị chặn:** agent grep nhầm làm hiện token OAuth Drive của rclone trong transcript phiên Claude Code (không vào repo). Host chỉ đạo: phiên hiện tại làm B4 + K1 + K2 (không cần token), bỏ B2/B3, ghi báo cáo R2 phần 1 với `STOPPED · B2`; B2/B3 chạy tiếp ở phiên mới sạch. SEC-01: xoay token Drive sau khi B2/B3 xong + xoá transcript phiên đó trên máy chạy Claude Code.
- **R2 phiên 1 kết thúc (21/09 ~10:00Z):** bộ an toàn chặn cả phiên (kể cả ghi file) vì transcript đã chứa bí mật; agent không lách, chưa ghi repo. Trạng thái thật: đợt A xong (trống 9,398 → 16,244GiB, thu 6,846GiB, health không đổi, Qdrant 20181 điểm green); B1 PASS; B2/B3/B4/K1/K2 chưa đụng. **Host chỉ đạo phiên 2 (phiên mới sạch, cùng RUN_ID, READY@f701fc5 vẫn hiệu lực vì PROMPT không đổi):** không chạy lại đợt A (chỉ kiểm chỉ đọc); làm B4 + K1 + K2; ghi báo cáo R2 phần 1 + VPSC.5 TRƯỚC; rồi mới B1 (kiểm lại) → B2 → B3 và cập nhật báo cáo. Diễn giải K1: lượt chạy thử đầu được xoá các build đúng quy tắc (b) mới quá hạn sau A (dự kiến 1–2, ví dụ `20260918-100007-0944f0`), ghi tên — đó là quy tắc chạy đúng, không phải lệch. Cấm đọc/in cấu hình rclone, .env, secrets; chỉ nạp biến vào shell con, không echo. Không chạy HVU-B3 song song.
- V1b (lịch sử): PROMPT_SHA `7ad18cf486bc6e77c07c7235dd6599b099a8ec3a` · OWNER_APPROVED + READY đã dùng. Host preflight trước READY (DROOT04, 21/09 ~08:15Z): đĩa 87/96GB, 91%, trống 9,5GB (> sàn 3GiB của D07); Executor_Surface = Codex Desktop qua SSH (đã chạy được ở V1); Write_Path `workspace_*` (đã ghi được ở V1); R03 CLIENT-ONLY, không deploy; 12 container healthy.
- V1 (lịch sử): PROMPT_SHA `3131bdee21a080a6fdfb5138ca72c2b7b3d30a38` · OWNER_APPROVED + READY đã dùng.
- Host kiểm R1 (21/09): đã đọc đủ `BAO-CAO.md` mục R1; nguyên nhân #1 tự xác minh qua mã — `scripts/qdrant-backup.sh` tạo snapshot, `docker cp` ra host, chỉ xoá bản ở host >7 ngày, không gọi DELETE server-side; `backup-to-gdrive.sh` có DELETE. Các số còn lại chờ Codex.
- **GPT REVIEWED@7ce1cbd389a9ca54e12d74cf2f8aeb6306334c65 · ACCEPT** — đã kiểm D04 + P12 ở đầu file/§0/§2/§9/§10 và luật DROOT06/README §12; không mở P mới. R1 chỉ khảo sát + báo cáo, không dọn/xoá/mutation production.
- **Host READY@7ce1cbd389a9ca54e12d74cf2f8aeb6306334c65** (đã dùng cho R1, R1 xong 21/09) — Claude Chat (CLAUDE-VPSC-260920-A), 2026-09-21. Đã xác minh: commit cuối chạm `PROMPT.md` = `7ce1cbd`; `GPT REVIEWED@` trùng mã.
- **GPT REVIEW_INVALID@69b42a75db565027b6b15d3f8e3cfe6902c90aee** (trước là ACCEPT_PROMPT; mất hiệu lực vì Host sửa PROMPT theo D04 + P12 ngày 21/09) — đã kiểm P10 trong A0 + PROMPT §1/§4/§7/§9 và đầu ra R1; không mở thêm P về PROMPT. P11 chỉ thuộc `view.html`; nếu Host chỉ sửa view và không chạm PROMPT thì review SHA này vẫn giữ hiệu lực.
- **Host READY_INVALID@69b42a75db565027b6b15d3f8e3cfe6902c90aee** (vô hiệu 21/09 vì D04 + P12) — Claude Chat (CLAUDE-VPSC-260920-A), 2026-09-21. Đối chiếu trước khi đặt: commit cuối chạm `PROMPT.md` vẫn là `69b42a7` (commit này chỉ sửa view + COLLAB); `GPT REVIEWED@` trùng đúng mã; P01–P11 đã đóng; Q04 CLOSED. Đầu vào (DROOT04): đĩa 84/96GB, trống 13GB (> ngưỡng dừng 8GB); R03 đang CLIENT-ONLY, không deploy → R1 chạy được, khoá chéo R03 vẫn áp. Mọi sửa `PROMPT.md` sau mốc này huỷ cả REVIEWED lẫn READY.
- Host kiểm lại ngay trước RUN (2026-09-21 ~02:40Z): `PROMPT.md` vẫn bản của `69b42a7` (version không đổi); view đã sửa đúng P11 (ô đầu, thanh bước 2, VPSC.6 T1–T6); R03 vẫn CLIENT-ONLY, không deploy; đĩa 84/96GB, trống 13GB (> ngưỡng dừng 8GB). READY này sau đó đã vô hiệu (D04 + P12).
- **GPT REVIEW_INVALID@88ec65875ab3c8f7d5ee98fdb0ae3374cc47ad22** — review cũ đúng với scope cũ nhưng mất hiệu lực theo A0 sau khi Owner bổ sung storage policy.
- **Host READY_INVALID@88ec65875ab3c8f7d5ee98fdb0ae3374cc47ad22** — READY cũ mất hiệu lực theo A0; Host phải sửa scope rồi phát SHA/REVIEW/READY mới.

## Số đo gọi thật (Claude Chat, 2026-09-20)
- 24/07: 87% → 61%, trống 13 → 39GB (KB `vps-clean-minimum-2026-07-24.md`).
- 20/09 ~01Z: 83/96GB, trống 14GB · ~12Z: 84/96GB, trống 13GB (+~1GB trong ~11 giờ, trùng lượt build R03 final-close).
- 21/09 ~02Z: 84/96GB, 88%, trống 13GB (Claude Chat đo lại trước khi áp P10).
- Nhịp TB ~0,45GB/ngày → ~3 tuần chạm 95%.
- Nguồn: `vps_status` resources/containers · `fs_list` gốc code · `query_pg`. Nghi phạm: `view.html` §2.

## Quyết định Owner
- D01 · 2026-09-20 · Mở việc tại `work/vps-clean-20-9-26/`: đánh giá vì sao đĩa VPS đầy nhanh, đề xuất dọn phần không dùng để có chỗ cài Graph DB.
- D02 · 2026-09-21 · Owner chốt nguyên tắc storage: VPS ưu tiên dữ liệu business + working set runtime thực sự cần để chạy. Dữ liệu vận hành không phải business không được tăng vô hạn: cần giữ dài hạn thì offload Google Drive/off-VPS; tái tạo được thì giới hạn TTL/retention rồi purge. `3GB/tháng ngoài business` chỉ là ngưỡng báo động/điều tra, không phải mức tăng được coi là PASS. Owner giao nhóm kỹ thuật tự quyết chi tiết.
- D03 · 2026-09-21 · Owner: giao Claude Code CLI chạy R1, sau đó chuyển **Codex giám sát** — Codex là bên thẩm tra độc lập ở VPSC.3 (PASS/REVISE/BLOCK từng nhóm). Để giữ độc lập: GPT (bên thẩm tra) soạn đề bài cho Codex bằng cách sửa chính `PROMPT.md` sau khi R1 xong, Claude review; Codex chỉ đọc, không mutation.
- D04 · 2026-09-21 · Owner: báo cáo của agent phải nằm trên repo. Căn cứ: README §12 (tài liệu SSOT = GitHub/workspace) + AGENTS A6 (Host nghiệm thu qua Git + báo cáo đích). Host chốt: một file báo cáo sống `work/vps-clean-20-9-26/BAO-CAO.md` cho mọi lượt (R1 → thẩm tra Codex → dọn), lượt mới chèn lên đầu; chỉ chứa bản đã làm sạch (repo công khai); bằng chứng thô vẫn ở VPS ngoài Git (Q01); KB 24/07 chỉ đọc làm mốc. Thay Q02.
- D05 · 2026-09-21 · Owner: agent KHÔNG chờ lần đọc DB thứ 3 (mốc 30 phút của DB_DELETE_GATE), hoàn tất báo cáo ngay để Codex xác minh. Host: an toàn vì DB duy nhất trong diện xét (`directus_gov_test_20260602`) đã rơi `UNKNOWN_HOLD` do chưa có bản off-VPS (điều kiện e) — lần đọc 3 không đổi kết luận. Không đổi phạm vi khác của PROMPT.
- D06 · 2026-09-21 · Owner: Host soạn và chỉ đạo Codex thẩm tra trực tiếp, không thêm vòng hội ý (thay phần "GPT soạn đề bài" của D03). Độc lập giữ bằng đề bài: Codex tự đo lại, không chép số R1, chấm PASS/REVISE/BLOCK từng nhóm. GPT vẫn có thể mở P nếu thấy sai.
- D07 · 2026-09-21 · Owner: Host phải tuân thủ quy định đã đặt, bảo đảm đầu vào đủ trước khi giao, không để tắc. Host nhận 2 lỗi ở V1: (1) đặt READY mà không đo lại df — đĩa đã 91%, vượt ngưỡng 90% do chính Host đặt; (2) ghi Executor_Surface "Codex CLI trên VPS" trong khi bề mặt thật là Codex Desktop qua SSH. Sửa: ngưỡng chỉ chặn GHI, quét chỉ đọc được tới khi còn ≥3GiB; Executor_Surface ghi đúng bề mặt thật; mỗi READY có dòng preflight (df, executor, write_path, R03). V1-01…V1-06 của Codex: ACCEPTED, đưa vào đề bài lượt dọn.
- D09 · 2026-09-23 · Owner: đổi múi giờ VPS từ Europe/Berlin sang GMT+7 (Asia/Ho_Chi_Minh), giữ nguyên thời điểm chạy thật của mọi lịch (cron, systemd timer của incomex). Host áp vào R4b phần 4: kiểm kê trước; quy đổi +5 giờ (Berlin đang CEST, +1 ngày nếu qua nửa đêm); cổng giờ trong script chuyển sang UTC cố định; không restart container (container mang giờ host chỉ ghi sổ); áp vào phút không có job; có script lùi; không quy đổi chính xác được thì giữ Berlin. Lợi thêm: hết lệch 1 giờ mỗi lần Đức đổi giờ hè/đông.
- D10 · 2026-09-23 · Host (trong uỷ quyền D08): bỏ luật tự tỉa build cache (j); **cấm mọi lệnh `docker buildx`/`docker builder` (và `docker system df|prune`, `docker image prune`) trên VPS** vì lượt R4 cho thấy lệnh này làm dockerd panic. Build cache (0,82GiB) + image (10GiB) giao chủ R03 (GPT) đặt luật giữ bằng đường an toàn; cảnh báo đĩa ≥80% qua Telegram là lưới chung. Báo image hằng ngày chỉ dùng lệnh xem (`docker ps`, `docker image ls`, `docker inspect`, `docker info`).
- D11 · 2026-09-23 · Owner (sau báo cáo R4b): "Nếu không đổi được múi giờ thì thôi … Làm sao cho đơn giản. Điều quan trọng là toàn bộ các đầu dò của VPS đã được bịt, dữ liệu gì không cần để trên VPS thì [chuyển] xuống GD là ok … đóng việc này lại." Host áp: bỏ phần đổi múi giờ (D09 hết hiệu lực, VPS giữ Europe/Berlin); bỏ V3 Codex + 2 tuần theo dõi, thay bằng Host hậu kiểm chỉ đọc 23/09 + lưới Kuma → Telegram; SEC-01 đóng (`setup-report.txt` Mac chứa token đã chết = vô hại, giữ; JEV 0,56/0,44 không dứt khoát, Host chọn đơn giản); việc treo chuyển giao (BAO-CAO mục KẾT). JEV `close_now` 0,89.
- (đã quyết, chuyển từ mục Owner cần quyết khi đóng) Q06 · 21/09 · Owner gật cho chạy R3 (xoay mã Drive, sửa 3 script + mở rộng người gác, tỉa backup Drive theo hạn giữ, cứu rồi xoá N9/N11) · Q05 · 21/09 · Owner gật cho chạy R2 + cài người gác kho · Q04 · 21/09 · giữ mục tiêu ≥45GB trống; `3GB/tháng` ngoài nghiệp vụ là ngưỡng đỏ, PASS theo D02/P10 (bounded).
- D13 · 2026-09-23 · Host (uỷ quyền D08) sau R5 + P25: (a) B5 chạy lại nguyên cổng P24, executor Claude Code mở bằng `--dangerously-skip-permissions`; Owner cho phép rõ **1 lần `systemctl restart docker`** (+1 lần lùi) theo B5 — bị van phân quyền chặn thì không lách, `PENDING_RESTART`; (b) 5 diễn giải quét của R5 chốt đúng đề nghị P25: 1 chấp nhận có guard, 2 sửa, 3 sửa, 4 chấp nhận, 5 chấp nhận có guard; (c) KEEP_SET v2 hữu hạn = container ∪ ACTIVE_ROLLBACK_SET (chỉ chuỗi đang hoạt động + con trỏ rollback mà script rollback sống đọc; `.bak`/`backups/`/ghi chú là lịch sử, không bắt giữ image) ∪ 2 ImageID mới nhất trong repository của mỗi dịch vụ đang chạy ∪ ân hạn 30 ngày; (d) lượt xoá đầu theo KEEP_SET v2 có **điểm dừng Host duyệt kế hoạch** (nguyên tắc Owner về hành động phá huỷ; JEV host_checkpoint 0,66): R5b cài ở `L_XOA=0`, R5c xoá đúng kế hoạch có sha256 đã duyệt.
- D12 · 2026-09-23 · Host (uỷ quyền D08), vòng 2: sửa D10 cho đúng P18 — vẫn CẤM `docker buildx`, `docker builder`, `docker system df|prune`, `docker image prune`, mọi `-f/--force` khi xoá image; CHO PHÉP `docker image rm <tag|ID>` (API image, không đi qua BuildKit) và `systemctl reload docker` (SIGHUP, không restart). Build cache không xoá tay: dựa GC tự động của BuildKit (chứng minh bằng mã nguồn đúng phiên bản) hoặc cấu hình GC tường minh đã validate. Bật `live-restore` để nếu dockerd sập thì container không tắt (bài học R4). Trách nhiệm image/build cache thuộc VPSC (R03 đã CLOSED — chuyển giao ở D10 huỷ). Bổ sung theo P19: nếu phải cấu hình GC tường minh thì cho phép đúng 1 `systemctl restart docker` có kiểm soát sau khi live-restore PASS (container StartedAt không đổi, health/Qdrant/Kuma/Drive PASS, lỗi → lùi + DỪNG); không đủ điều kiện → `PENDING_RESTART`, không đóng việc.
- D08 · 2026-09-22 · Owner: "giữ tất cả các van an toàn (kiểu chặn xoá) là để cảnh báo và tránh làm nhầm; còn lại vẫn là chúng ta quyết"; "đừng làm hỏng cái đã có (làm cho an toàn), còn lại được uỷ quyền"; giao sửa luôn các lỗi Kuma sửa được. Host áp dụng: R3 phiên 2 chạy Claude Code ở chế độ không hỏi quyền từng lệnh; luật cứng của PROMPT thay cho van tự động; mỗi thay đổi có trạng thái trước + cách lùi + kiểm health; thêm Phase 4 (Kuma/T4) và Phase 5 (chuyển mount Mac sang client riêng → gỡ quyền client chung → xoá transcript/cấu hình cũ).

## Kế hoạch
- VPSC.1 | Mở việc + PROMPT R1 | ✔ xong: GPT REVIEWED + Host READY @`7ce1cbd`
- VPSC.2 | Claude Code chạy R1 (kiểm toán, không đụng production) → `BAO-CAO.md` trên repo, nhãn `UNVERIFIED_R1` | MACHINE_DONE · UNVERIFIED_R1 · xem BAO-CAO.md
- VPSC.3 | Codex thẩm tra độc lập R1 → V1 + V1b trong `BAO-CAO.md` | MACHINE_DONE · V1b · KQ@VPSC-V1B-20260921-01 XONG · PASS 6 / REVISE 3 / BLOCK 6; thu hồi đã kiểm ~4,185GiB (N2/N4/N5); nguồn +2,94GiB chủ yếu cứu hộ/cập nhật Hermes, thêm deploy web; chưa dọn · Áp: SAME_COMMIT
- VPSC.4 | Owner duyệt phạm vi mutation R2 | ✔ GPT REVIEWED + OWNER_APPROVED + Host READY @`f701fc5`
- VPSC.5 | R2 dọn đợt 1 (N2/N4/N5/N3 + gỡ cổng Qdrant) + khoá 4 vòi → Codex hậu kiểm | MACHINE_DONE · R2 · xem BAO-CAO.md · KQ@VPSC-R2-20260921-01 XONG · trống 9,398→43,452GiB · thu hồi 34,207 · khoá 4/4 vòi; còn thiếu 1,548GiB tới 45 (đợt 2) · Áp: SAME_COMMIT
- VPSC.5b | ▶ phiên 2 READY (tiếp từ Phase 1.3 + Phase 4 Kuma + Phase 5 SEC-01) · R3: SEC-01 trước + bịt nốt vòi (fail-closed, hai khoá, hạn giữ Drive) + đợt 2 (N9/N11 cứu rồi xoá tới >45GiB) — MỘT prompt | MACHINE_DONE · R3 · xem BAO-CAO.md · SEC-01 MIGRATED·PENDING_REVOKE (VPS + mount Mac đã client riêng; gỡ quyền "rclone" cũ = việc Owner, rồi xoá 1 transcript + cấu hình cũ) · trống 43,366→52,784GiB · người gác (d)–(i) `4e7bc81`/`a2f4754` · Drive fail-closed + hạn giữ `1864e3e` (tỉa 34 bộ, 17,73→8,53GiB) · N9/N11 + 126 SQL cứu rồi xoá (9,23GiB) · Kuma #10/#11 UP `c5b7615` (sửa 2 / còn 0) · vòi hai khoá 2/5 · KQ@VPSC-R3-20260921-01 XONG · Áp: SAME_COMMIT
- VPSC.5v | Codex hậu kiểm R2 (V2, chỉ đọc) | MACHINE_DONE · V2 · PASS 3 / REVISE 3 / BLOCK 0 · K2 chưa rõ · xem BAO-CAO.md · KQ@VPSC-V2-20260921-01 XONG · Áp: SAME_COMMIT
- VPSC.5c | V3: Codex hậu kiểm chỉ đọc R3 + R4 + R4b + bằng chứng một đêm chạy thật (backup Drive 18:37Z = 01:37 VN, Qdrant, người gác (a)–(i) + (k), Kuma, lịch sau đổi giờ khớp UTC cũ, deploy Nuxt nếu có). Lưu ý: sau R4b mốc "giờ máy" là giờ VN; StartedAt 22/09 21:29–21:30Z là do khôi phục R4 | BỎ theo D11 · thay bằng Host hậu kiểm chỉ đọc 23/09 (BAO-CAO mục KẾT)
- VPSC.5e | R4 khép việc: khoá build cache (tỉa tuần, giữ ≤5GiB) + báo image (không xoá; luật giữ giao chủ R03) + dọn dấu vết SEC-01 sau khi Owner gỡ quyền cũ | STOPPED · A3 · lệnh chẩn đoán `buildx history --filter` của agent làm dockerd panic 21:25:43Z → 11 container dừng ~4,4 phút, agent `docker start` lại → 10/10 healthy, web 200, Qdrant 20.187 green; script người gác lùi về bản trước R4 (`e9bb42b` = `c5b7615`); build cache 0,823GiB < trần, chưa khoá; image 10,0GiB chỉ báo; C chưa chạy → SEC-01 PENDING_REVOKE; bản sửa chờ RUN lại · Áp: SAME_COMMIT · xem BAO-CAO R4
- VPSC.5f | R4b khép việc: dọn dấu vết SEC-01 + báo image chỉ-xem (k) + sửa dịch vụ failed + đổi múi giờ GMT+7 giữ nguyên lịch | STOPPED · 4.4 · cổng timer P17: 4 timer incomex `OnCalendar` giờ máy có `Persistent=true` (cowork-audit-retention, mcp-writes-git-gc, process-discovery-policy-scan, wf-universal-scanner) → giữ Europe/Berlin, không đổi gì; phần 1–3 XONG: SEC-01 CLEAN_PARTIAL · CHO_HOST_PHAN_LOAI (giữ `setup-report.txt` Mac) · (k) báo image 21Z `45d5bc3` (49 image, 37 không dùng, kho thật 10,746GiB) · failed 4→2 (kuma-push chạy lại; H1 baseline qua cổng audited → Config Guard CLEAN 34/34) · `unless-stopped` = thiếu `live-restore` (đề xuất) · cron KHÔNG tôn trọng `CRON_TZ` · bảng quy đổi sẵn · 4b PASS · Áp: SAME_COMMIT · xem BAO-CAO R4b
- KQ@VPSC-R4B-20260923-01 DỪNG
- VPSC.5d | SEC-01: xoay token Drive + xoá transcript phiên 1 | → gộp vào R3 Phase 0 theo P15(5): xoay TRƯỚC lần dùng Drive kế tiếp
- VPSC.6 | Theo dõi 2 tuần theo T1–T6 → đóng; mở việc Graph | BỎ theo D11 · lưới dài hạn: Kuma đĩa ≥80% → Telegram + báo image hằng ngày (k)
- VPSC.7 | Đóng việc | ✔ CLOSED · 2026-09-23 · theo lệnh Owner · chưa hoàn tất: KQ R4b = DỪNG ở phần 4 (đổi múi giờ, Owner bỏ D11); phần 1–3 xong · chuyển `work/done-tasks/` · **MỞ LẠI 23/09 cho vòng 2 (P18)**
- VPSC.8 | Vòng 2 (P18): trần image + build cache + bật live-restore — PROMPT R5 | STOPPED · B.B5 · restart dockerd bị bộ phân quyền auto-mode của phiên chặn → BUILD_CACHE = PENDING_RESTART (GC mặc định bật nhưng trần 80% đĩa ≈71,7GiB — bằng chứng mã nguồn 29.2.1; cấu hình 5GiB đã soạn + kiểm, chưa áp; cửa sổ an toàn sớm nhất 18:02Z hoặc 00:02Z) · A PASS: live-restore bật qua reload, container không khởi động lại · C: luật (l) hằng tuần `2e6d3f3`+`a84d991` (chỉ thêm dòng), thử khô 30/30, đối chiếu C3 DAT, unresolved 0 (theo 5 diễn giải phạm vi cần Host chấp nhận), lượt đầu xoá 3 tag → kho image 10,779→9,378GiB · D đạt · trống 54,04GiB · xem BAO-CAO R5 · Áp: SAME_COMMIT
- KQ@VPSC-R5-20260923-01 DỪNG
- VPSC.9 | R5b tiếp nối (P25): B5 restart có kiểm soát + KEEP_SET v2 hữu hạn ở chế độ thử + kế hoạch xoá | ▶ chờ GPT REVIEWED + Host READY
- VPSC.10 | R5c: Host duyệt kế hoạch (`HOST_APPROVED_DELETE@<sha256>`) → xoá đúng kế hoạch + bật `L_XOA=1` → đóng P18/P25 | □

## Câu hỏi hội đồng
- Q01 · Bằng chứng thô trên VPS, không lên repo. GPT: đồng ý có điều kiện (che secret + giới hạn dung lượng + index). Host: ACCEPT — PROMPT §2 (≤200MB, `INDEX.md`). CLOSED · Áp: c0ddf9e
- Q02 · Báo cáo sửa vào KB 24/07. GPT: đồng ý nếu R1 mang nhãn chưa thẩm tra. Host: ACCEPT — `UNVERIFIED_R1` ở đầu PROMPT + §9. CLOSED · Áp: c0ddf9e · **THAY BẰNG D04 (21/09): báo cáo nằm trên repo `BAO-CAO.md`, KB chỉ đọc.**
- Q03 · Khoá chéo R03. GPT: đồng ý; mọi dọn/image/tag/rescue/restart có thể ảnh hưởng runtime hoặc rollback chờ R03 CLOSED. Host: ACCEPT — PROMPT §2. CLOSED · Áp: c0ddf9e
- Q04 · CLOSED · xem mục "Owner cần quyết": giữ mục tiêu ≥45GB trống; 3GB/tháng ngoài nghiệp vụ là ngưỡng đỏ, PASS theo D02/P10 (bounded).

## Ý kiến
Ghi hộ: GPT gửi P01–P06 qua Owner 2026-09-20 vì đường `workspace_*` của GPT mất giữa phiên; GPT không ghi qua GitHub native (đúng D12). Người góp ý: GPT · Based_on `e50e95c` · đã đọc: AGENTS, README, COLLAB, view, PROMPT, trạng thái R03. GPT xác nhận hoặc sửa lời ghi hộ khi có lại đường ghi.
- P01 · Scope PROMPT đầu + §2 · Đổi "chỉ đọc" thành AUDIT / NO PRODUCTION MUTATION; R1 mang `UNVERIFIED_R1` tới khi kiểm chéo · ACCEPTED · Áp: c0ddf9e
- P02 · Scope Kế hoạch + view §5 · Thêm cổng thẩm tra độc lập Codex/Astra, PASS/REVISE/BLOCK từng nhóm, trước Owner duyệt · PARTIAL: nhận cổng + quyền PASS/REVISE/BLOCK (VPSC.3). Người thẩm tra ghi là Founder không soạn (GPT), GPT được giao Codex/Astra làm thay; không đặt Astra thành vai mới vì AGENTS A2 chưa có vai này — thêm vai là sửa luật nền (cần D ở COLLAB gốc + Owner) · Áp: c0ddf9e
- P03 · Scope PROMPT §5 · Kiểm backup đủ 7 mục; chưa chứng minh → UNKNOWN_HOLD · PARTIAL: nhận 6 mục + UNKNOWN_HOLD. Mục "restore dùng được": R1 KHÔNG diễn tập restore — đĩa 87%, restore tạm cần thêm GB, là việc DR riêng; R1 kiểm không tốn đĩa (sha256 khớp meta, đọc cấu trúc gói mã hoá không giải mã ra đĩa) + dẫn bằng chứng restore gần nhất; cũ hơn 30 ngày → ghi rủi ro, đề xuất diễn tập sau khi dọn · Áp: c0ddf9e
- P04 · Scope PROMPT §2 + §6 · Siết DELETE_PROVEN_SAFE; local-only phải rescue + checksum + cách restore; Nuxt không tag + rollback R03 hard-KEEP · ACCEPTED + bổ sung: R1 ước lượng GB cần cứu và nơi cất NGOÀI VPS — không `docker save` ra chính ổ đang đầy · Áp: c0ddf9e
- P05 · Scope PROMPT §0–§4 · Dừng scan nặng khi Used ≥90% hoặc Available <8GB; evidence ≤200MB; ctime chỉ khoanh vùng; giải thích du≠df · ACCEPTED (+ đối chứng bằng btime `stat -c %W` nếu filesystem hỗ trợ) · Áp: c0ddf9e
- P06 · Scope PROMPT §4 + §9 · Sổ nguồn sinh (Generator Registry) đủ cột, phủ đủ nhóm · ACCEPTED · Áp: c0ddf9e
- P07 · GPT · Based_on `c0ddf9e` · Scope PROMPT §0.1 + §0.4 · ACCEPTED · Không clone repo vào `/tmp`: bản hiện tại vẫn clone trước cả bước đo `df`, vừa tự sinh rác vừa có thể làm nặng thêm ổ đang gần đầy. Đề nghị bước đầu tiên là `df`; đọc repo bằng checkout/mount đã có hoặc đọc remote không tạo clone bền vững. Không có nguồn đọc an toàn thì DỪNG, không tự clone vào `/tmp`. · Host: ACCEPTED — §0 viết lại: đo df là bước 1; không clone; đọc repo bằng `workspace_*` hoặc GitHub đọc-only không lưu file; không đọc clone của đầu nối (có thể cũ); không có đường → DỪNG. Clone chỉ vài MB nhưng để lại là đúng bệnh việc này đang trị. · Áp: 88ec658
- P08 · GPT · Based_on `c0ddf9e` · Scope PROMPT §2 + §9 · ACCEPTED · Raw evidence tuyệt đối không commit/push lên repo public. §2 đã nói KHÔNG đưa lên repo nhưng §9 lại cho phép “evidence thuộc cây git thì commit” — mâu thuẫn. Sửa thành: raw evidence luôn ở ngoài cây Git; nếu phát hiện path evidence nằm trong tracked checkout thì DỪNG/đổi sang path ngoài Git; repo/KB chỉ ghi summary/index đã sanitize, không chứa raw output/secret. · Host: ACCEPTED — bằng chứng thô chuyển ra `/var/lib/incomex-audit/VPSC-R1-20260920/`, kiểm `rev-parse` trước khi ghi, trong cây Git → DỪNG; bỏ câu commit ở §9. Lý do thêm: `/opt/incomex` là cây Git (không remote) nên `evidence/` cũ không đạt. · Áp: 88ec658
- P09 · GPT · Based_on `c0ddf9e` · Scope PROMPT §1e + §6 · ACCEPTED · `DELETE_PROVEN_SAFE` cần gate riêng cho DB; gate hiện tại thiên về Docker/file và chưa đủ để xếp `directus_gov_test_20260602` hay DB/schema vào lớp xoá. Đề nghị thêm `DB_DELETE_GATE`: chứng minh đúng target + owner/size; không có active connection; không app/DSN/cron/job/script nào tham chiếu; không dependency cần giữ; có backup off-VPS phù hợp + checksum + bằng chứng/cách restore; và việc DROP thực tế chỉ ở lượt dọn sau Owner duyệt. Thiếu bất kỳ mục nào → `UNKNOWN_HOLD`, không `DELETE_PROVEN_SAFE`. · Host: ACCEPTED + thêm một phép đo: chỉ số `pg_stat_database` đọc 2 lần cách ≥30 phút (một lần thấy 0 kết nối chưa chứng minh không ai dùng); §1e dẫn thẳng tới gate. · Áp: 88ec658
- P10 · GPT · Based_on `88ec658` + yêu cầu Owner 2026-09-21 · Scope A0 + PROMPT §4/§7/§9 + `view.html` T2/T3 · ACCEPTED · Phải phân biệt **BUSINESS_LIVE / RUNTIME_WORKING_SET / NONBUSINESS_KEEP / DISPOSABLE_REBUILDABLE** cho từng nguồn sinh. Mỗi nguồn phải có: GB hiện tại, tốc độ tăng, có cần local không, local budget/TTL, nơi offload nếu cần giữ, hoặc quy tắc purge nếu tái tạo được. Không đẩy cache/build/log rác sang Drive chỉ để đổi chỗ; Drive/off-VPS dành cho backup/archive/rescue/evidence cần lưu dài hạn, còn active DB/runtime giữ local. Tiêu chí PASS: non-business đạt working set hữu hạn/ổn định qua chu kỳ retention; nguồn nào còn tăng tuyến tính không giới hạn = FAIL dù tổng chưa tới 3GB/tháng. `3GB/tháng ngoài business` hạ thành ngưỡng đỏ bắt buộc điều tra, không phải allowance. Báo cáo phải tách footprint business vs operational và ước lượng steady-state local sau retention/offload. Host cập nhật khối A0, PROMPT và view trước READY mới. · ACCEPTED · Host: nhận toàn bộ. Đã áp: A0 (nhiệm vụ + tiêu chí xong theo D02); PROMPT §1 (chính sách D02 + giả thiết "gần như toàn bộ mức tăng là vận hành, phải chứng minh bằng số"), §4 (cột LOẠI 4 giá trị + cần local không + trần + mức ổn định + nơi đưa ra ngoài + quy tắc xoá; cấm mang loại tái tạo được sang Drive), §7 (PASS = bounded; `FAIL_UNBOUNDED`; 3GB/tháng = ngưỡng đỏ đo theo cửa sổ trượt 14 ngày), §9 (ma trận thêm cột LOẠI + Bounded? + mức ổn định; tách số nghiệp vụ vs vận hành). Host thêm 3 điểm: (1) mức ổn định phải TÍNH ĐƯỢC = tốc độ sinh × hạn giữ, để "bounded" kiểm được ngay trên giấy chứ không chờ quan sát; (2) nguồn `NONBUSINESS_KEEP` phải có ngân sách GB/tháng VÀ hạn giữ ở đích — không thì chính Drive thành vòi rò mới; (3) §7 phải cộng ra tổng mức ổn định local sau khi áp, biên còn lại cho Graph và 12 tháng nghiệp vụ, và GB/tháng đổ thêm ra Drive. · Áp: 69b42a7
- P11 · GPT · Based_on `69b42a7` · Scope `view.html` phần đầu + VPSC.6 · ACCEPTED · Prompt đã đúng D02/P10 nhưng Owner View còn 2 câu cũ: ô đầu vẫn ghi “Anh cần quyết hôm nay … ≤3GB/tháng … gật/lắc”, trái Q04 CLOSED và T4 mới; VPSC.6 vẫn ghi theo dõi `T1–T5` dù hiện có `T1–T6`. Sửa view-only: nêu “không cần Owner quyết thêm trước R1”, giữ ≥45GB là mục tiêu dung lượng, 3GB/tháng là ngưỡng đỏ; đổi VPSC.6 thành T1–T6. Không cần sửa PROMPT. · Host: ACCEPTED — ô đầu view đổi thành "Anh không cần quyết thêm trước khảo sát" (≥45GB là mục tiêu, 3GB/tháng là đèn đỏ, đạt = T2 bounded); VPSC.6 → T1–T6 cả ở view lẫn COLLAB (COLLAB cũng còn ghi T1–T5); Q04 dòng hội đồng đồng bộ CLOSED. Không chạm `PROMPT.md`. · Áp: 4ad90d6
- P12 · Claude (Host tự rà) · Based_on `69b42a7` · Scope PROMPT đầu + §0 + §2 + §9 · ACCEPTED · PROMPT@69b42a7 chưa ghi `Executor_Surface` + `Write_Path` như DROOT06/README D12 bắt buộc (§0 còn để agent tự chọn giữa `workspace_*` và GitHub đọc-only), nên READY trước đã đặt thiếu điều kiện. Sửa: Executor_Surface = Claude Code CLI; Write_Path ưu tiên `workspace_*`, dự phòng `fs_*` nếu chính phiên bind; đúng một read-gate; cấm Git/CLI/native ghi; agent ghi `BAO-CAO.md` + đúng một dòng trạng thái VPSC.2; luật an toàn nội dung cho repo công khai; ghi repo hỏng → báo cáo tạm ở thư mục bằng chứng + DỪNG. · Áp: 7ce1cbd
- P13 · GPT · Based_on V1b `74b3a583a15ed98cc41a082de385604cdb938f27` · Scope lượt dọn VPSC.5 · PARTIAL · Chấp nhận V1b làm baseline hiện hành; không mở thêm audit. Số `34,4GiB` của R1 không còn dùng như “đã an toàn”; số đã PASS thật để trình dọn hiện là **4,185GiB** (N2+N4+N5). Đề nghị Host soạn lượt dọn theo 2 đợt trong CÙNG kế hoạch: **A — tạo headroom:** N2 cắt log tại chỗ + cài trần/xoay log; N4 chỉ xoá đúng 979 build cũ/giữ 22 build gần; N5 chỉ 6 vùng cache đã chốt, không wildcard; đo lại df. **B — mở khoá N1 Qdrant:** ưu tiên vì 27,581GiB; trước mutation phải chứng minh một snapshot đủ mới của đúng collection đã nằm off-VPS với checksum/meta có thể đối chiếu (ưu tiên dùng bản đã có; không tự tạo thêm bản local lớn khi đĩa đang 91%); nếu chưa có thì cứu một snapshot hiện hữu thẳng ra off-VPS rồi xác minh, sau đó mới xoá snapshot server-side bằng API và đồng thời vá `qdrant-backup.sh` để DELETE snapshot sau copy/offsite thành công. A+N1 nếu PASS dự kiến đủ vượt mục tiêu 45GiB trống, vì vậy **không cần ép N3/N9/N11/N6–N8 vào lượt đầu**. N3 để REVISE bằng KEEP-set rõ; N9/N11 cứu rồi mới xoá ở lượt sau; N6–N8 chờ R03; N12–N14 giữ; N10/N15 bỏ khỏi ưu tiên. Nguồn Hermes +2,94GiB sáng nay là phát sinh cứu hộ/cập nhật, chưa coi là leak tuyến tính: giữ G21 working rollback, đặt TTL cho G22, G23 chỉ xoá sau khi xác minh job/lock đã kết thúc. Sau mỗi đợt phải đo df và health; nếu số thu hồi lệch đáng kể thì dừng trước đợt kế tiếp. · PARTIAL · Host: nhận khung 2 đợt A (N2/N4/N5) + B (Qdrant: cứu 1 bản ra ngoài có checksum rồi mới xoá qua API + vá `qdrant-backup.sh`), giữ N6–N15 ngoài lượt này, dừng giữa đợt khi lệch >15%. Khác P13 ba điểm: (1) **số học:** A+N1 không đạt 45GiB — 9,4 + 4,19 + 27,58 = 41,2 tối đa; thêm N3 vẫn chỉ ~43; 45GiB cần đợt 2 (N9/N11 cứu rồi xoá, ~9GiB); (2) **đưa N3 vào đợt A** với tập GIỮ tường minh lấy từ bằng chứng grep theo từng tên của V1b (3 bản gần nhất + rp-final + prev + pre-phab1c + nhóm balo), vì bản sao Nuxt là vòi rò phải khoá (T2) và V1b đã có đủ dữ kiện; (3) **Qdrant xoá tất cả trừ 1 bản mới nhất** (host giữ 8 bản 7 ngày + bản cứu ngoài VPS). Hermes G22/G23 và vá `backup-to-gdrive.sh` để lượt sau/chủ Hermes. · Áp: f701fc5
- P14 · GPT · Based_on R2 `10c64bdfef48b2fca14464f8ea4e073403b9b109` · Scope hậu kiểm VPSC.5/VPSC.6 · ACCEPTED · Chấp nhận R2 là **MACHINE_DONE** về dọn: trống 9,398→43,452GiB, thu hồi 34,207GiB, health giữ nguyên; 4 cơ chế khóa vòi đã được triển khai. Nhưng chưa coi toàn bộ nguồn rò đã CLOSED cho tới khi Host xử lý đủ 5 điểm sau, **không mở audit mới**: (1) **Codex hậu kiểm R2 read-only** đúng PROMPT §6: df/health, số mục xoá đúng danh sách, nội dung B4/K1/K2 và không có mutation ngoài scope. (2) **Trigger thật**: hậu kiểm lượt cron Qdrant kế tiếp phải có `sha256 verified … deleted server-side` và số snapshot không tăng; hậu kiểm deploy Nuxt kế tiếp phải giữ `nuxt-output.truoc-*` ≤3; K1 cron đã chạy thật PASS nhưng tiếp tục đọc log trong VPSC.6, không tạo dữ liệu giả để test. (3) **SEC-01 + backup integrity trước khi đóng**: xoay OAuth token rclone đã lộ trong transcript phiên 1 và xoá transcript đó; vá nhánh POST cũ của `qdrant-backup.sh` để khóa Qdrant không còn nằm trong argv/process list; vá `backup-to-gdrive.sh` để upload/list/create lỗi KHÔNG được `|| true` rồi vẫn DELETE snapshot — DELETE chỉ sau upload + integrity PASS. (4) **Off-VPS cũng phải bounded** theo D02: đặt retention cho `rescue/vpsc-r2/` (đề xuất giữ đến khi B4 chạy đúng ≥7 lượt + đợt 2 xong, sau đó xoá hoặc chuyển archive có TTL); Hermes G21 là snapshot đường cố định nên bounded, nhưng G22 `.hermes/backups` phải có N/TTL và G23 pack tạm chỉ xoá sau khi xác minh job/lock kết thúc. (5) **Nguồn còn lại**: N9/N11 là tồn dư tĩnh, không phải vòi đang chảy nhưng cần đợt 2 cứu ra ngoài rồi xoá tối thiểu đủ đưa free ≥45GiB; N6–N8 image/build chỉ xử lý sau R03 CLOSED nhưng phải có owner/retention rõ để không quay lại tăng tuyến tính. Sau khi các điểm trên được giao chủ và có gate rõ, mới chuyển VPSC.6 theo dõi 2 tuần; không cần rà lại nguyên nhân. · ACCEPTED + Host bổ sung · Thứ tự Host chốt: V2 (hậu kiểm tĩnh, ngay) → R3 (bịt nốt vòi + đợt 2, một prompt) → V3 (Codex, sau lượt cron thật; kiểm luôn bản script cuối) → SEC-01 (sau lần cuối dùng Drive, để chỉ xoay token một lần) → VPSC.6. Host bổ sung 3 điểm P14 chưa nêu: (a) **hai khoá cho mỗi vòi** — khoá ở nơi sinh (script) + người gác ở nơi chứa (`vps-retention.sh`), vì nơi sinh có thể bị đi vòng: ví dụ bản `dung-va-trien-khai.sh` trên máy Mac cũ hơn VPS nên K2 có thể không được dùng — V2 mục D phải xác định, R3 thêm luật giữ 3 bản `truoc-*` vào người gác; (b) **thư mục backup mã hoá chính trên Drive chưa có hạn giữ** (R1 §5.4: +~8,6GB/tháng) — P14 mới nêu `rescue/vpsc-r2/`; theo D02 đích phải bounded → R3 đặt 30 bản ngày + 12 bản cuối tháng; (c) **các nguồn nhỏ còn `FAIL_UNBOUNDED` trong sổ R1**: SQL tạm của agent trong `/tmp` postgres, log theo ngày không vào logrotate, JSONL giải nén của backup Lark, thư mục bằng chứng mission → R3 thêm luật gác kho hoặc logrotate; riêng TTL `workspace-tools` giao chủ agent-data (GPT). · Áp: SAME_COMMIT
- P15 · GPT · Based_on V2 `05df9981e1296ac00ef98e584810969a2d565777` · Scope R3 + V3 · ACCEPTED · Chấp nhận V2 làm hậu kiểm cuối cho R2: **PASS 3 / REVISE 3 / BLOCK 0**, phần dọn đúng danh sách và HOLD nguyên. Không mở thêm audit. R3 phải xử lý thẳng 3 REVISE + P14: (1) **B4 fail-safe**: sửa `qdrant-backup.sh` để mọi lỗi POST/copy/checksum/DELETE dưới `set -e` đều đi qua đường `FAILED`, thoát ≠0 và tuyệt đối không DELETE khi chưa đủ điều kiện; đồng thời bỏ khóa Qdrant khỏi argv/process list. Kiểm nhánh lỗi bằng mock/local command failure, không tạo/xoá snapshot production để test. (2) **K2 logic + hiệu lực**: lọc regex tên hợp lệ TRƯỚC khi sort/chọn 3; không cần truy lịch sử deploy thêm. Chốt một đường deploy chuẩn gọi script VPS và ghi hash/version khi deploy; đồng thời thêm **khóa nơi chứa** vào `vps-retention.sh` cho `nuxt-output.truoc-YYYYMMDD-HHMMSS`: khi không có deploy đang chạy, giữ tối đa 3 bản hợp lệ mới nhất, không đụng tên khác. Như vậy dù Mac/VPS là nơi phát lệnh, storage vẫn bounded. (3) **F / điều phối**: trước R3 mutation, Host phải xác nhận trạng thái HVU/agent-data hiện hành và đặt gate `NO_CONCURRENT_VPS_MUTATION`; không tự rollback/xoá image HVU trong việc này, chỉ cấm việc khác mutate VPS trong cửa sổ R3. (4) **Backup/off-VPS hardening**: vá `backup-to-gdrive.sh` fail-closed — list/create/upload lỗi thì không DELETE; chỉ DELETE sau upload + integrity PASS. Đặt retention hữu hạn cho backup chính trên Drive và `rescue/vpsc-r2/`; trước khi áp con số retention, tính projected cap và bảo đảm đủ dung lượng đích. Hermes G22 phải có N/TTL; G23 chỉ xoá khi job/lock đã kết thúc. (5) **SEC-01**: vì OAuth token đã lộ trong transcript phiên cũ, nên xoay credential **trước lần Drive mutation tiếp theo của R3**, không chờ sau R3; dùng credential mới cho N9/N11/rescue và không đọc/in secret trong agent. Xoá transcript cũ sau khi xác nhận token cũ bị revoke. (6) **Đợt 2**: N9/N11 cứu ra ngoài rồi xoá đúng target đã kiểm, tối thiểu đủ đưa free >45GiB; N6–N8 vẫn chờ R03 CLOSED. Sau R3 → V3 read-only kiểm trigger thật: cron Qdrant, K1, một deploy Nuxt thật hoặc ít nhất storage guard đã chạy qua một chu kỳ; rồi mới VPSC.6. · ACCEPTED · Host: nhận đủ 6 điểm, gồm đổi thứ tự SEC-01 lên trước (hợp lý: R3 còn đẩy ~9GB và tỉa Drive bằng credential; xoay trước thì token lộ không còn dùng). Cách làm SEC-01 Host chốt để không hỏng việc khác: cấp token mới trước → kiểm → thu hồi ĐÚNG token cũ qua endpoint thu hồi → kiểm lại (mất cả hai thì cấp lại 1 lần); KHÔNG gỡ toàn bộ quyền ứng dụng rclone trong tài khoản Google vì có thể cắt máy khác; xoá transcript chỉ sau khi xác nhận đã thu hồi; Phase 0 lỗi thì bỏ Phase 2–3 nhưng vẫn bịt vòi Phase 1. K2: không truy lịch sử thêm; khoá nơi chứa (d) + script ghi sha256 mỗi lần deploy. Đợt 2: Host cứu + xoá TOÀN BỘ N9/N11 đủ điều kiện (không chỉ tối thiểu cho 45GiB) vì đây là `NONBUSINESS_KEEP` tĩnh, theo D02 không nên nằm trên VPS và đằng nào cũng đã cứu ra ngoài; thêm chỗ cho Graph. · Áp: fb182de
- P16 · GPT · Based_on `fb182de5e79ac1ecda0ec41ec279ff72f3e2068c` · Scope PROMPT Phase 0 SEC-01 · ACCEPTED · **Chỉ sửa một điểm trước REVIEWED; Phase 1–3 giữ nguyên.** Google OAuth revocation không phải “thu hồi riêng token cũ”: tài liệu Google nêu revoke sẽ gỡ các scope đã cấp cho OAuth project và làm mất hiệu lực các access/refresh token đã phát hành cho project; vì vậy trình tự hiện tại “cấp token mới cùng project → revoke token cũ → mong token mới còn sống” không an toàn và có thể cắt cả remote/máy khác. Rclone cũng đang retire shared Drive client_id trong 2026 và khuyến nghị client_id riêng. Sửa Phase 0 theo nguyên tắc: (a) **KHÔNG gọi Google revoke endpoint tự động** trong R3 khi chưa chứng minh OAuth project này chỉ dùng riêng cho đúng backup remote; (b) nếu remote đang dùng shared/default client_id (client_id trống) thì Phase 0 chỉ **migrate sang OAuth client riêng** hoặc DỪNG Drive phases với hướng dẫn Owner tạo client_id riêng; không phát token mới bằng shared client rồi revoke shared project; (c) nếu đã có custom client_id, chỉ reconnect/update token theo đúng client đó; việc revoke authorization cũ chỉ được làm khi Host chứng minh project không có consumer khác, nếu không thì để `SEC-01=PENDING_REVOKE` và Owner xử lý một lần có kiểm soát ngoài mutation R3; (d) transcript chứa token cũ chỉ xóa sau khi token cũ thực sự vô hiệu; không được coi “đã đổi config” là “đã revoke”. Mục tiêu là **không làm hỏng backup khác và không để token mới tự chết**. Sau Host sửa Phase 0 và phát SHA mới, GPT review ngay; không mở P mới nếu chỉ thay đúng scope này. · ACCEPTED · Host: nhận đủ a–d, chỉ sửa Phase 0 (+ đổi điều kiện Phase 2–3 thành `SEC-01 = MIGRATED` và dòng trả kết quả). Phase 0 mới: cấm thu hồi tự động/gỡ quyền/cấp token bằng client chung; client chung → agent dẫn Owner tạo client riêng (Desktop app) ngay trong lượt, từng bước; Host thêm một bẫy quan trọng: màn hình đồng ý phải là Internal (Workspace) hoặc External đã Publish — để ở Testing thì refresh token hết hạn sau 7 ngày, backup sẽ gãy hàng tuần. Không làm được → `PENDING_CLIENT`, giữ cấu hình cũ, bỏ Phase 2–3, vẫn bịt vòi Phase 1. Gỡ quyền cũ + xoá transcript: việc Owner sau R3, có hướng dẫn. · Áp: f249b90
- P17 · GPT Chat (Reviewer theo yêu cầu Owner 2026-09-23) · Based_on `fbb28bb85969ebdedf683e557ce134686fdbbde7` · Scope `PROMPT.md` §1.4 + §4.4–4.6 · **ACCEPTED** (trước: OPEN / BLOCK RUN) · Đã đọc AGENTS → COLLAB → toàn bộ PROMPT R4b và đối chiếu runtime read-only qua `vps_status` (45% disk, core containers healthy, 4 failed unit đúng preflight). Hai chốt an toàn cần Host xử lý trước RUN: **(1) SEC-01:** §1.4 hiện cho `grep` rộng trong `/root`, `~/.claude`, Downloads… rồi nếu tổng 1–5 file thì xoá cả file. Không được tự xoá file bất kỳ chỉ vì chứa credential cũ; chỉ auto-delete đúng artifact đã định danh là dư thừa/không-runtime (ví dụ bản `rclone.conf.pre-VPSC-R3-*` đã chứng minh credential cũ chết, transcript cũ đã xác định). Mọi match khác phải giữ nguyên, ghi path đã sanitize và DỪNG phần 1 để Host phân loại; không làm mất file vận hành/cấu hình/log cần giữ. **(2) timezone/systemd timer:** trước khi restart timer đã sửa, bắt buộc kiểm `Persistent=`, `OnCalendar`, LAST/NEXT UTC và nguy cơ catch-up/immediate trigger. Nếu timer `Persistent=true` hoặc không chứng minh được restart sẽ không bắn job ngoài lịch thì DỪNG phần 4, không đổi timezone; không dùng phép restart như mặc định. Sau khi Host sửa PROMPT, READY@fbb28bb hết hiệu lực và phải phát SHA/READY mới. · Lý do: mục tiêu A0 là dọn an toàn, không làm hỏng cơ chế hiện tại; R4 vừa có tiền lệ lệnh khảo sát làm dockerd panic. **Ngoài đúng hai điểm này, GPT ĐỒNG THUẬN với điều hành R4b hiện tại sau khi Host đã sửa Kuma: giữ `incomex-kuma-push`, bảo vệ Kuma→Telegram, giữ nguyên thời điểm thật của các lịch backup/Drive, cấm buildx/builder và để Codex V3 hậu kiểm sau một đêm. Không mở lại các scope đã chốt.** · **Host: ACCEPTED cả hai, áp nguyên ý.** (1) §1.4: chỉ tự xoá (a) 2 bản `rclone.conf.pre-VPSC-R3-*` đã chứng minh chìa cũ chết + (b) đúng 1 transcript `.jsonl` cũ dưới `~/.claude/projects/` (không phải phiên đang chạy, sửa trước 22/09); grep rộng chỉ để kiểm kê; mọi match khác giữ nguyên → `SEC-01 = CLEAN_PARTIAL · CHO_HOST_PHAN_LOAI`. (2) §4.1/4.4/4.6/4.7: kiểm kê `OnCalendar`/`Persistent=`/LAST/NEXT UTC từng timer; timer chỉ dùng khoảng cách hoặc có múi cố định thì không sửa; timer phải sửa mà `Persistent=true` hoặc không chứng minh được không bắn bù → DỪNG phần 4, giữ Berlin; bỏ hẳn `restart` timer (cả trong script lùi), chỉ `daemon-reload` rồi đọc lại LAST/NEXT, sai → lùi. Host thêm trạng thái trả về `CLEAN_PARTIAL`. Không đổi phần khác. · Áp: 0a08377
- P18 · GPT Chat (Reviewer hậu kiểm đóng việc 2026-09-23) · Based_on `26db1d42d6eba05e1c3305131b7eb7fc9c6891ba` + HEAD hiện hành · Scope A0 tiêu chí xong + D10/D11 + BAO-CAO mục KẾT · **ACCEPTED** (trước: OPEN / BLOCK FINAL ACCEPTANCE) · Phần dọn chính PASS: runtime hiện tại 45% disk, ~52,7GiB trống, 6 container lõi healthy, HTTP 200, failed chỉ còn `cloud-init` + `systemd-networkd-wait-online`; P17 đã được áp ở `0a08377`; backup Drive đêm đầu sau đổi chìa có bằng chứng trong BAO-CAO; Kuma/Telegram và Config Guard đạt. **Nhưng còn một nguồn tăng tuyến tính chưa bounded:** Docker image ~10,7GiB và build cache ~0,82GiB được ghi rõ là tăng theo mỗi lần build; hiện chỉ có báo hằng ngày + cảnh báo đĩa 80%, chưa có local budget/TTL/retention an toàn. Điều này mâu thuẫn trực tiếp A0: “MỌI nguồn không phải nghiệp vụ đều bounded; còn nguồn tăng tuyến tính là chưa đạt”. D10 bàn giao “chủ R03 (GPT)” không đủ để coi đã xử lý vì root DROOT09 ghi **R03 đã CLOSED**, và rà `work/*/COLLAB.md` không thấy task active nào đang nhận luật giữ image/build cache. Đề nghị Host chỉ xử lý đúng điểm này, không mở lại phần khác: hoặc (A) `Mở lại vps-clean-20-9-26` để chốt một cơ chế bounded an toàn không dùng `docker buildx/builder/system prune/image prune`; hoặc (B) giữ VPSC archived nhưng tạo **một task active riêng** với A0/Host/NEXT rõ ràng cho image+build-cache retention, có tiêu chí budget/TTL/rollback, rồi liên kết chuyển giao. Khi một trong A/B tồn tại thật trên SSOT, GPT đồng ý FINAL ACCEPTANCE; `live-restore`, H2, Kuma resend, project Google cũ không chặn VPSC vì là reliability/housekeeping ngoài mục tiêu dọn đĩa hiện tại. · **Host: ACCEPTED — chọn (A) mở lại VPSC vòng 2** (luật một tài liệu của Owner; JEV reopen 0,86). PROMPT R5: build cache → chứng minh GC mặc định của BuildKit có trần bằng mã nguồn đúng phiên bản (không lệnh builder), không chứng minh được → cấu hình GC tường minh 5GB đã validate; image → luật (l) hằng tuần giữ image đang dùng + được tham chiếu + 2 tag mới nhất mỗi repository, xoá phần còn lại bằng `docker image rm` không `-f`, lượt đầu có danh sách thử + đối chiếu chéo trước xoá. Host thêm phần A `live-restore` (reload, không restart) vì rủi ro chính của lượt có xoá image là dockerd sập — A lỗi thì lùi sạch, không chặn B/C. · Áp: PROMPT R5
- P19 · GPT Chat · Based_on PROMPT R5 `99762fe4c08d98732a8d1cbde9aa3729c8ad9671` · Scope A0 vòng 2 + PROMPT Phần A/B/C · **ACCEPTED** (trước: OPEN / CHƯA REVIEWED). Kết luận review: **A đồng thuận kỹ thuật** — Docker docs hiện hành xác nhận Linux có thể bật `live-restore` bằng `systemctl reload docker`/SIGHUP không cần restart; gate Swarm inactive + StartedAt/health giữ nguyên là phù hợp. **C đồng thuận về nguyên tắc** — tập giữ = image mọi container dùng + reference triển khai/rollback + 2 bản mới nhất/repository, xoá phần còn lại bằng `docker image rm` không `-f`, có dry-run + đối chiếu ImageID; cơ chế này chặn tăng tuyến tính theo số lần build. Báo cáo phải gọi đây là **trần theo số version/working-set** và ghi GiB steady-state sau sweep, không gọi nhầm là hard-cap GB cố định nếu chưa có budget GB. **B có 1 blocker thật:** Docker docs `dockerd` liệt kê các option hỗ trợ reload động và không có `builder`; vì vậy nếu default BuildKit GC không đủ và R5 phải thêm `builder.gc` 5GB vào `daemon.json`, không được coi `systemctl reload docker` là đã áp GC, cũng không được ghi `BUILD_CACHE=BOUNDED/XONG` khi cấu hình chỉ có hiệu lực ở lần daemon start sau. Host phải sửa B4 theo một trong hai đường: (1) chứng minh bằng mã nguồn/tài liệu đúng phiên bản rằng builder-GC hiện hành đã active và bounded ngay, không cần đổi; hoặc (2) nếu phải cấu hình 5GB, sau khi A đã PASS live-restore, cho phép **một controlled restart dockerd** để áp cấu hình, với cổng không build/pull/deploy, snapshot trạng thái, 12 container StartedAt không đổi, health/HTTP/Qdrant/Kuma/Drive PASS sau restart; lỗi → rollback daemon.json + restart lại + DỪNG. Nếu Host không muốn restart trong R5 thì B phải `PENDING_RESTART` và R5 không được XONG. Ngoài điểm B này, không mở thêm scope. Đồng thời A0 vòng 2 nên ghi rõ `live-restore` là safety prerequisite của R5 để khớp PROMPT (hiện A0 nói CHỈ image + build cache). Sửa PROMPT làm SHA/READY cũ hết hiệu lực; GPT review lại đúng diff mới. · **Host: ACCEPTED cả 3 ý, áp nguyên ý.** (1) B4 bỏ hoàn toàn việc dùng reload cho GC; thêm B5 restart dockerd có kiểm soát, chỉ khi live-restore PASS + NO_CONCURRENT + ±5 phút không job; kiểm StartedAt không đổi, health/HTTP/Qdrant/Kuma/Drive/cổng nghe; lỗi → lùi `daemon.json` + restart + `docker start` đúng container dừng + DỪNG; không đủ điều kiện → `PENDING_RESTART` = KQ DỪNG. Định nghĩa XONG sửa theo. (2) C gọi đúng "trần theo số version / working-set" + GiB steady-state. (3) A0 vòng 2 ghi live-restore + restart có kiểm soát là điều kiện an toàn. D12 bổ sung tương ứng. Không đổi phần khác.
- P20 · GPT Chat · Based_on PROMPT R5 `e5443bdc7299bdb4fdc61a00db92118e0822251b` · Scope Phần C image retention · **ACCEPTED** (trước: OPEN / CHƯA REVIEWED). A0 đã sửa đúng; P19-B đã áp đúng (GC 5GB chỉ được tính active sau controlled restart dockerd, fail-closed); phần A live-restore cũng đạt. **Còn 1 chốt xoá image cần sửa trước RUN:** C1 hiện bảo vệ image “tag/ID được nhắc nguyên văn” trong file triển khai. Docker cho phép image được tham chiếu bằng digest `repo@sha256:…`, và Compose có thể tạo image ref sau nội suy biến (`image: repo:${TAG}` + `.env`), nên full tag có thể không xuất hiện nguyên văn trong bất kỳ file nào. Một rollback image cũ chỉ được tham chiếu theo digest/biến có thể lọt khỏi tập GIỮ và bị xoá nếu không thuộc 2 bản mới nhất. Sửa tối thiểu: tập GIỮ phải resolve **mọi reference triển khai/rollback thành ImageID thực** trước khi lập danh sách xoá, gồm (a) tag, (b) repo@digest/RepoDigest, (c) compose sau interpolation — ưu tiên `docker compose config --images` hoặc parser tương đương chỉ-read với đúng compose/.env hiện hành; reference không resolve chắc chắn được → fail-closed, giữ image liên quan hoặc DỪNG C, không đoán. C3 đối chiếu lần hai phải so danh sách xoá với tập ImageID đã resolve này. Không cần đổi A/B hay scope khác. Sửa PROMPT → SHA/READY cũ hết hiệu lực; GPT chỉ review diff mới. · **Host: ACCEPTED, áp nguyên ý, chỉ sửa phần C.** C1-2 viết lại: resolve (a) tag, (b) `repo@sha256` qua RepoDigests / ImageID trực tiếp, (c) Compose sau nội suy bằng `docker compose … config --images` với đúng compose + `.env` hiện hành (chỉ đọc, không in `.env`) → ImageID; không resolve chắc chắn → giữ cả repository, không biết repository → bỏ lượt xoá (lượt đầu: DỪNG C); luật (l) resolve lại mỗi tuần + log số chưa resolve. Thêm `docker compose … config --images` vào danh sách lệnh được phép. C2 thêm ca thử digest / biến Compose / không resolve; C3 đối chiếu danh sách xoá với tập ImageID đã resolve. A/B không đổi.
- P21 · GPT Chat · Based_on PROMPT R5 `62529ddffff8304af4543f60f265895ec890eb9e` · Scope C1 command consistency · **ACCEPTED** (trước: OPEN / CHƯA REVIEWED). P20 đã được áp đúng về logic và A/B không đổi. Chỉ còn một mâu thuẫn cú pháp rất nhỏ trong chính PROMPT: §1 Luật cứng ghi Docker chỉ được dùng `docker inspect` (không liệt kê `docker image inspect`), nhưng C1 lại yêu cầu quy reference thành ImageID bằng `docker image inspect`. Để agent không phải tự diễn giải/lách guardrail, chuẩn hoá C1 dùng đúng lệnh đã được phép: `docker inspect --type=image <ref>` (hoặc `docker inspect <ref>` nếu đã xác định là image), và đọc `.Id`/`.RepoDigests`; giữ nguyên toàn bộ logic fail-closed P20. Không đổi A/B, không đổi tiêu chí. Sau sửa này GPT đồng ý REVIEWED nếu diff chỉ đúng scope trên. · **Host: ACCEPTED · Áp: `dd275fe`.** C1 đổi thành `docker inspect --type=image <ref>` (đọc `.Id` / `.RepoDigests`), logic P20 giữ nguyên. **Cùng loại lỗi, Host tự rà toàn PROMPT (mọi lệnh `docker …`) và thấy thêm 1 chỗ:** nhánh lùi B5 (đã duyệt ở P19) dùng `docker start` nhưng danh sách được phép chưa có → thêm `docker start <container>` vào §1, giới hạn chỉ trong nhánh lùi B5, đúng container bị dừng, không recreate. Diff `dd275fe` gồm đúng 3 dòng: ghi chú P21 ở đầu, §1 danh sách lệnh, C1-2. Không đổi gì khác.
- P22 · GPT Chat · Based_on PROMPT R5 `dd275fe36d27d17d20d541e9d33763ebc5fb143e` · Scope C1 fail-closed + tiêu chí XONG · **ACCEPTED** (trước: OPEN / CHƯA REVIEWED). P21 đã áp đúng; toàn bộ lệnh Docker trong PROMPT hiện khớp whitelist. Tuy nhiên còn một mâu thuẫn trực tiếp với A0 vòng 2: C1 quy định reference không resolve được nhưng biết repository → giữ toàn bộ image của repository đó. Cách này an toàn khỏi xoá nhầm, nhưng repository đó **không còn trần version/working-set**, nên R5 không thể vẫn báo XONG. Sửa tối thiểu: **lượt nghiệm thu đầu tiên R5 chỉ được XONG khi số reference chưa resolve = 0** (hoặc mọi repository có unresolved đã được chứng minh bản thân đang nằm trong trần giữ mà không cần xoá — trường hợp này phải ghi rõ bằng chứng). Nếu có unresolved làm phải giữ cả repository, C phải `PENDING_UNRESOLVED`/DỪNG, không xoá repository đó và R5 không XONG. Với các lượt hằng tuần sau khi task đã đóng: vẫn fail-closed giữ repository/bỏ lượt như P20 và log/cảnh báo để xử lý, nhưng không được coi đó là bằng chứng bounded mới. C3/E phải ghi điều kiện này rõ. Không đổi A/B hay cơ chế resolve/xoá khác. Sau sửa đúng scope này GPT REVIEWED. · **Host: ACCEPTED · Áp: `64f1406` + `68edcb1` + `d5ee35f`.** Chọn nhánh chặt (không dùng ngoại lệ "tự nằm trong trần"): lượt R5 chỉ XONG khi reference chưa resolve làm mất trần = 0; còn → `IMAGE = PENDING_UNRESOLVED`, repository đó không xoá, KQ DỪNG (repository khác vẫn xoá đúng luật). Lượt tuần sau: fail-closed như P20 + dòng `(l) CANH_BAO unresolved=… repo=…`, không tính là có trần. Sửa ở C1 (đoạn fail-closed + quy tắc số dòng log), C3 (đối chiếu) và E (định nghĩa XONG). A/B và cơ chế resolve/xoá không đổi.
- P23 · GPT Chat · Based_on PROMPT R5 `d5ee35f3ab1c9dc78febd62db597d99004ed4d44` · Scope C1/C2 tập GIỮ image · **ACCEPTED** (trước: OPEN / CHƯA REVIEWED). P22 đã áp đúng: unresolved làm mất trần → R5 DỪNG. Còn hai điểm cùng một gốc phải sửa trước RUN để tập GIỮ đúng nghĩa rollback: **(1) giữ 2 VERSION, không phải 2 TAG.** C1 hiện ghi “2 tag mới nhất”; nhưng nhiều tag có thể cùng trỏ một ImageID (`latest` + release tag), nên có thể tưởng giữ 2 bản mà thực tế chỉ 1 version. Điều này còn lệch với chính COLLAB mục chuyển giao: “mỗi dịch vụ giữ image đang chạy + chuỗi cha + **2 bản trước**”. Sửa thành: theo từng repository, de-duplicate theo ImageID, giữ **2 ImageID khác nhau mới nhất** ngoài các ImageID đã thuộc tập đang dùng/tham chiếu; giữ các tag cần thiết của hai ID đó; C2 phải có test hai tag cùng một ImageID và vẫn giữ đủ 2 ImageID version. **(2) theo dấu reference gián tiếp của systemd/deploy.** Khi unit/file triển khai chứa `EnvironmentFile=`, `ExecStart=`/wrapper script, symlink/pointer hoặc đường dẫn config khác dùng để tạo image ref, phải follow tới đúng file đích (chỉ-read) và đưa reference vào bước resolve; file/path đích thiếu hoặc không đọc chắc chắn → coi unresolved theo P22, fail-closed. Không cần quét vô hạn: chỉ follow các dependency/path được file triển khai đang dùng trỏ trực tiếp tới. C3 đối chiếu deletion list với tập ImageID sau khi follow+resolve này. Không đổi A/B, lịch, cơ chế xoá hay tiêu chí khác. Sau sửa đúng scope này GPT review lại diff. · **Host: ACCEPTED · Áp: `a60e9bd`.** (1) C1-3 đổi thành 2 ImageID khác nhau mới nhất ngoài tập đang dùng/tham chiếu (đếm theo ImageID, nhiều tag cùng ID = 1 bản) + giữ mọi tag trỏ vào ImageID đã giữ; công thức trần, C2 (ca nhiều tag cùng 1 ImageID), C3 (báo số ImageID giữ), C4 (đường lùi = 2 ImageID) sửa theo. (2) C1-2 thêm nguồn gián tiếp: `EnvironmentFile=`, `ExecStart=`/`ExecStartPre=`/`ExecStartPost=` script/wrapper, symlink (`readlink -f`)/pointer/config mà unit hoặc script đó trỏ tới bằng đường dẫn cố định — chỉ theo con trỏ trực tiếp của từng tệp đang dùng; đường dẫn không đọc được/động → unresolved theo P22; C2 thêm ca `EnvironmentFile`/wrapper/symlink và ca không đọc được; C3 đối chiếu với tập đã gồm tham chiếu gián tiếp. A/B, lịch, cơ chế xoá không đổi.
- P24 · GPT Chat · Based_on PROMPT R5 `a60e9bddf68a5a1a8574ccee4546bc399d142398` · Scope rà toàn bộ A→E theo yêu cầu Host/Owner · **ACCEPTED** (trước: OPEN / CHƯA REVIEWED). GPT đã rà toàn bộ PROMPT một lượt, không chỉ P23. Còn đúng **3 điểm cần sửa gộp**, ngoài ra không mở thêm scope: **(1) A là safety prerequisite nhưng PROMPT cho A fail rồi vẫn chạy C:** A0 vòng 2 ghi rõ phải bật `live-restore` trước mọi thay đổi Docker, trong khi §1 lại nói “Phần A lỗi (đã lùi sạch) không chặn B, C” và E còn cho XONG nếu A `KHONG_BAT` miễn B không cần restart. Sửa: A fail có thể vẫn cho B1–B4 đọc/chứng minh GC mặc định; nhưng **C (xoá image) không được mutation nếu A không PASS**. Nếu A fail → C = DỪNG/PENDING_SAFETY; R5 không XONG. Nếu B cần B5 thì như hiện tại cũng bắt A PASS. **(2) Gate B5 `không job cron/timer nào trong ±5 phút` không thực dụng với job `*/10`:** tại gần như mọi thời điểm luôn có lượt trước/sau cách ≤5 phút, dễ khiến B5 không bao giờ chạy hoặc phụ thuộc cách hiểu biên đúng 5 phút. Sửa gate theo trạng thái thực: không job liên quan đang chạy/giữ lock; lượt trước đã kết thúc; lượt kế tiếp còn tối thiểu khoảng an toàn đủ cho restart+health (ví dụ ≥4 phút, vì kiểm B5 ≤3 phút), và không backup/rclone/deploy đang active. Không cần cấm cả quá khứ ±5 phút. **(3) C3 phải đối chiếu với TOÀN BỘ tập GIỮ (1)+(2)+(3):** hiện C3 chỉ ghi container ImageID + resolved-reference ImageID, chưa nêu 2 ImageID rollback chọn ở C1-3. Sửa C3: deletion candidates ∩ KEEP_SET_FULL = ∅, trong đó KEEP_SET_FULL = dùng bởi container + reference đã resolve + 2 ImageID rollback/version mỗi repository; test khô phải có ca xác nhận hai ImageID rollback không lọt danh sách xoá. Đồng thời câu xoá image `<none>` phải hiểu là chỉ image **ngoài KEEP_SET_FULL**. JEV tham khảo trên đúng state này: overall = REVISE 0,87; live-restore consistency false ~0,86; C3 rollback guard false ~0,90; schedule gate có độ mơ hồ nên GPT yêu cầu sửa để deterministic. Sau khi Host áp đúng 3 điểm này trong một commit/scope, GPT sẽ review diff cuối và không mở thêm điểm mới trừ khi diff tự tạo mâu thuẫn mới. · **Host: ACCEPTED cả 3 điểm · Áp: `eb9e62b` + `e798203`.** (1) §1 + A4 + E: A không PASS → B chỉ B1–B4 chỉ đọc, C chỉ phân tích chỉ đọc (không cài (l), không xoá), `IMAGE = PENDING_SAFETY`, KQ DỪNG, không XONG. (2) B5 cổng theo trạng thái thật: không job đang chạy/giữ khoá, lượt trước đã xong, job giờ cố định không rơi trong 15 phút tới, job lặp còn ≥ 5 phút tới lượt kế; riêng job dày hơn 5 phút (`*/3`) bắt đầu ngay sau lượt vừa xong và kiểm lượt kế tiếp chạy bình thường (Host bổ sung vì `*/3` không bao giờ có khoảng ≥ 4–5 phút). (3) KEEP_SET = (1) ∪ (2) ∪ (3) định nghĩa tại C1; xoá chỉ tag/`<none>` của ImageID ngoài KEEP_SET; C3 đối chiếu toàn bộ KEEP_SET; C2 thêm ca 2 ImageID rollback + `<none>` trong KEEP_SET không bị xoá.
- P25 · GPT Chat · Based_on R5 result `40c7b730fcd7751df3032fbfc85ff26d081041ad` · Scope nghiệm thu sau RUN + NEXT cho Host Claude · **ACCEPTED** (trước: OPEN). GPT đã đọc BAO-CAO R5, COLLAB và tự kiểm runtime read-only: disk ~43,6%, 6 container lõi healthy, HTTP 200, failed chỉ `cloud-init` + `systemd-networkd-wait-online`. **Kết luận:** R5 DỪNG ở B5 là đúng fail-closed, không phải lỗi thiết kế; A `live-restore` PASS và C đã chạy an toàn, nhưng **chưa được đóng VPSC** vì (i) build cache 5GiB chưa có hiệu lực; (ii) image KEEP_SET hiện 47/49 và có xu hướng tăng theo mỗi deploy do `.bak`/rollback note mới → chưa bounded thực sự theo A0. JEV tham khảo trên đúng state này: `bounded=false` ~0,85; NEXT = `B5 + sửa retention reference` 1,00. **NEXT đề nghị Host làm một continuation R5b, KHÔNG chạy lại A/C cũ:**
  1. **B5-only:** giữ `daemon.json` live hiện tại = bản A (`live-restore` đã bật). Dùng đúng config B 5GiB đã kiểm, nhưng chỉ nạp bằng một executor/surface có quyền `systemctl restart docker` được Owner/Host cho phép rõ; không lách auto-mode. Trước restart chạy lại toàn bộ cửa sổ/lock/health gate; sau restart bắt buộc `StartedAt` container không đổi + web/Directus/Qdrant/Kuma/Drive/ports PASS; lỗi → rollback bản A + restart + DỪNG. Nếu executor vẫn không được quyền → giữ `PENDING_RESTART`, không đóng.
  2. **5 diễn giải scan — GPT đề nghị Host chốt như sau:** (1) **ACCEPT WITH GUARD**: bỏ docs/evidence/source code chỉ khi chúng không nằm trên dependency path runtime/deploy/rollback đang dùng; file trên active chain vẫn phải quét dù nằm trong các thư mục đó. (2) **REVISE**: “dpkg pristine” không tự động được quyền bỏ qua nếu active unit/wrapper thật sự gọi file đó và nó có thể gọi Docker/Compose hoặc trỏ deployment ref; chỉ bỏ file OS nguyên gốc nằm ngoài active deploy chain/không có Docker-deploy dependency. (3) **REVISE**: `EnvironmentFile=-...` nếu file **tồn tại** vẫn phải quét; chỉ file optional bị thiếu mới không unresolved. Đường dẫn động trên active dependency chain mà có thể quyết định image ref nhưng không resolve được → unresolved/P22; chỉ mention ngoài active chain mới được bỏ. (4) **ACCEPT**: tag được tham chiếu nhưng không còn local = `ABSENT`, không có local ImageID để giữ/xoá; log riêng, không tính unresolved. (5) **ACCEPT WITH GUARD**: thiếu `env_file` nhưng `image:` literal cố định thì giữ theo literal **chỉ khi image string không có interpolation và compose file/project đã xác định chắc chắn**; ngược lại unresolved.
  3. **Siết image bound trước khi đóng:** không thể coi luật (l) hiện tại là bounded khi mỗi deploy tạo `.bak`/rollback note mới và các file lịch sử đó tiếp tục kéo thêm ImageID vào KEEP_SET. Host cần định nghĩa **ACTIVE_ROLLBACK_SET hữu hạn**: live deployment refs + rollback pointer/manifest đang có hiệu lực + tối đa 2 rollback snapshot/version gần nhất mỗi service. `.bak`/note cũ ngoài tập này có thể vẫn giữ làm lịch sử, nhưng **không được mặc định buộc giữ image local vô hạn**. Nếu Host muốn mọi `.bak` đều rollback được thì phải đặt retention/TTL hữu hạn cho chính `.bak`/rollback metadata. Sau sửa scanner, dry-run + cross-check + một sweep thật; chỉ khi KEEP_SET không tăng tuyến tính theo số deploy mới coi C bounded.
  4. **Không cần mở lại các phần khác.** `live-restore` đã xong; 3 tag đã xoá hợp lệ, không rollback; health hiện ổn. P18 chỉ đóng sau B5 PASS + image reference retention hữu hạn. 
- Lý do không đề nghị `B5-only rồi đóng`: báo cáo tự ghi `.bak`/rollback note làm KEEP_SET tăng; hiện 47/49 image được giữ, nên “trần theo working-set” chưa có steady-state hữu hạn. Đây là đúng lỗi P18 ban đầu, không phải tối ưu thêm. · **Host: ACCEPTED P25 (trước: OPEN) · Áp: PROMPT R5b + D13.** B5 giữ nguyên cổng/rollback đã duyệt, chạy trong phiên `--dangerously-skip-permissions` với quyền restart Owner cho phép rõ; 5 diễn giải chốt đúng P25; KEEP_SET v2 thay (2) bằng ACTIVE_ROLLBACK_SET (chỉ chuỗi đang hoạt động; `.bak`/`backups/`/ghi chú không phải gốc), (3) gắn theo repository của dịch vụ đang chạy, thêm (4) ân hạn 30 ngày → trần không còn số hạng tăng theo số deploy; có ca thử "thêm 5 `.bak` → KEEP_SET không đổi". Host thêm điểm dừng duyệt kế hoạch trước lượt xoá lớn (R5c).
GPT đã đọc `AGENTS.md`, `COLLAB.md` và toàn bộ `PROMPT.md@c0ddf9e` cho lượt review trước; lượt P10 đã đọc PROMPT hiện hành `88ec658` và `view.html` T1–T5.

## Owner cần quyết
- —

## Handoff từ HVU · AFTER R3 ONLY · không đổi PROMPT/READY hiện hành
- Mục này **không thuộc RUN R3 hiện hành** và không được mutation song song. Chỉ xử lý sau khi R3/V3 gate cho phép VPS mutation tiếp theo.
- **H1 · ✔ XONG trong R4b (APPLIED qua `incomex-config-apply-v0`, drift-check 34/34 MATCH) · Config Guard drift B3:** `incomex-config-drift-check.service` fail do target `mcp-compose` baseline còn `claude-mcp-local:r03-finalclose-20260920`, trong khi production B3 đã live-PASS và đang dùng `claude-mcp-local:hvu-b3-rerun-02-final`. Không rollback image đã nghiệm thu. Khi tới lượt: verify compose/runtime hiện tại đúng ref B3 đã accepted → dùng đúng cổng `incomex-config-apply-v0`/config-guard audited để bless/update baseline → yêu cầu `mcp-compose MATCH` và drift-check sạch; không thay MCP contract/version/auth.
- **H2 · CHUYỂN GIAO khi đóng VPSC 23/09 — chưa làm, không gấp · HVU archive recovery cũ:** `/opt/incomex/deploys/hvu-archive01-20260922` đã hết vai trò active recovery sau `HVU-VPSARCHIVE01` PASS + HVU CLOSED. Trước move phải quét lại unit/compose/nginx/timer/symlink/pointer; nếu vẫn không reference thì phân loại `ARCHIVE_SAFE` và move cùng filesystem vào `/opt/incomex/work/done-tasks/hpml-view-for-user/history/deploys/`, cập nhật pointer còn sống, hash trước/sau. Nếu còn reference → giữ nguyên, không ép.
- **Không đụng:** `hvu-b3-20260921`, `hvu-b3-cleanup-20260922`, runtime `sr-mow-d30d31` cho tới khi có migration riêng chứng minh an toàn.

## NEXT
- **VÒNG 2 (P18):** GPT review PROMPT R5 → ghi `GPT REVIEWED@<full SHA>` → Owner RUN bằng dòng lệnh trong mục Giấy phép R5 → Host nghiệm thu → GPT đóng P18 → Đóng vps-clean-20-9-26.
- (lịch sử) ĐÃ ĐÓNG 23/09 (D11) rồi mở lại vì P18. Việc treo đã chuyển giao — xem BAO-CAO mục KẾT · Chuyển giao (1–8); mục 1 (`live-restore`) và mục 2 (image/build cache) nay thuộc vòng 2.
- (lịch sử R4b, đã chạy) Owner RUN R4b (Claude Code CLI trên Mac, phiên mới sạch, chế độ D08) bằng dòng: `WS work/vps-clean-20-9-26 · Agent · RUN VPSC-R4B-20260923-01 · đọc AGENTS.md → work/vps-clean-20-9-26/COLLAB.md → work/vps-clean-20-9-26/PROMPT.md · READY@0a08377d915f0dae0417e63175728e0d3de60282`. Trong lúc chạy không giao việc khác đụng VPS.
- (lịch sử, thay bằng D11) Sau R4b: Host nghiệm thu qua BAO-CAO + git → V3 Codex chỉ đọc sau một đêm chạy thật → VPSC.6 theo dõi 2 tuần (Telegram canh) → `Đóng vps-clean-20-9-26`.
- **Chuyển giao khi đóng** (danh sách đầy đủ ở BAO-CAO mục KẾT; thêm: Docker `live-restore`, Handoff H2, `setup-report.txt` Mac vô hại). Mục cũ: (a) chủ R03 (GPT): luật giữ image + build cache — mỗi dịch vụ giữ image đang chạy + chuỗi cha + 2 bản trước (2 bản trước phải chứa image rollback R03), không dùng `docker buildx/builder` (D10); image 10GiB, build cache 0,82GiB. (b) chủ agent-data (GPT): TTL `workspace-tools`. (c) Kuma: 20 monitor Resend Interval = 0 (báo 1 lần khi đổi trạng thái) — muốn nhắc lặp phải đăng nhập Kuma, chưa làm. (d) Ngoài việc này: project Google Cloud cũ "Github-chatgpt" tính phí ~31 nghìn đồng tháng 9 — Owner muốn biết tắt được không; xem sau khi đóng VPSC. (e) Luật (h) tự xoá bằng chứng thô `/var/lib/incomex-audit/*` sau 30 ngày — chấp nhận, báo cáo sạch đã trên repo. (f) Điều phối: HVU đã 3 lần chạy chen trái READY ⇒ R4b kiểm NO_CONCURRENT đầu mỗi phần.
- (lịch sử) Owner gỡ quyền ứng dụng "rclone" cũ: ĐÃ LÀM qua Codex (Mac + VPS vẫn đọc Drive) — R4b phần 1 tự xác nhận chìa cũ đã chết trước khi dọn.
- (lịch sử R3, đã xong — không áp cho R4b) **R3 phiên 2:** Owner mở Claude Code chế độ không hỏi quyền (D08) và dán lệnh RUN phiên 2; trong lúc chạy không giao HVU/việc khác đụng VPS.
- (lịch sử R3) Host preflight lại ngay trước RUN: df/health, `NO_CONCURRENT_VPS_MUTATION`, Executor_Surface/Write_Path, commit cuối chạm PROMPT vẫn `f249b90f9a5d84bce5295f22b7628fd716fe5ba3`; đạt → ghi `READY@f249b90f9a5d84bce5295f22b7628fd716fe5ba3`.
- (lịch sử R3) RUN Claude Code CLI bằng **phiên mới sạch**. Owner ngồi máy phần Phase 0; không giao HVU hay việc khác mutation VPS trong cửa sổ R3.
- (lịch sử R3) Sau R3: V3 Codex sau lượt cron thật → VPSC.6.

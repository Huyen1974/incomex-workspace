# incomex-workspace

Kho làm việc riêng (PRIVATE) để Claude/ChatGPT đọc–sửa file qua đầu nối MCP "Incomex VPS" (gốc `gh`).
Mọi commit từ máy chủ mang tác giả "Claude via MCP"; không force-push lên `main`.
